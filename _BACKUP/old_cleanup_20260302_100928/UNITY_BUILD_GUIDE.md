# 🏗️ Unity Build Guide — Drag Racing

**Последнее обновление:** 27 февраля 2026 г.

---

## 📋 Требования

| Компонент | Версия | Статус |
|-----------|--------|--------|
| Unity | 6000.x (Unity 6) | ✅ Установлен |
| .NET SDK | 10.0+ | ✅ Установлен |
| PowerShell | 5.1+ | ✅ Встроен в Windows |

---

## 🎯 Быстрая сборка

### Вариант 1: Через Unity Editor (Рекомендуется)

1. **Откройте проект в Unity:**
   ```bash
   # Через Unity Hub
   Добавить → Выбрать папку: D:\QwenPoekt\ProbMenu\DragRaceUnity
   
   # Или через PowerShell
   .\unity-open.ps1
   ```

2. **Настройте сцены:**
   ```
   File → Build Settings
   ```
   
   Добавьте сцены в правильном порядке:
   - [ ] `Assets/Scenes/MainMenu.unity` (индекс 0)
   - [ ] `Assets/Scenes/GameMenu.unity` (индекс 1)
   - [ ] `Assets/Scenes/Race.unity` (индекс 2)
   - [ ] `Assets/Scenes/Garage.unity` (индекс 3)
   - [ ] `Assets/Scenes/Tuning.unity` (индекс 4)
   - [ ] `Assets/Scenes/Shop.unity` (индекс 5)

3. **Автоматическая настройка сцен (опционально):**
   ```
   Tools → Drag Racing → Setup All Scenes
   ```

4. **Сборка:**
   ```
   Tools → Drag Racing → Build → Windows x64
   ```
   
   Или для всех платформ:
   ```
   Tools → Drag Racing → Build → All Platforms
   ```

5. **Готово!** Билд в папке `DragRaceUnity/Builds/`

---

### Вариант 2: Через PowerShell скрипт

```bash
# Быстрая сборка
.\unity-build.ps1

# С очисткой предыдущих билдов
.\unity-build.ps1 -clean

# Свой путь для билда
.\unity-build.ps1 -buildPath "C:\MyBuilds\DragRacing.exe"

# Помощь
.\unity-build.ps1 -help
```

---

### Вариант 3: Через командную строку Unity

```bash
# Windows
"C:\Program Files\Unity\Hub\Editor\6000.0.0f1\Editor\Unity.exe" ^
  -batchmode ^
  -nographics ^
  -projectPath "D:\QwenPoekt\ProbMenu\DragRaceUnity" ^
  -executeMethod ProbMenu.Editor.AutoBuildScript.BuildWindowsX64 ^
  -quit ^
  -logFile build-log.txt
```

---

## 📁 Структура билда

```
Builds/
└── DragRacing-WinX64/
    ├── DragRacing.exe          # Запускаемый файл
    ├── DragRacing_Data/        # Данные игры
    │   ├── globalgamemanagers
    │   ├── level0
    │   ├── resources.assets
    │   └── ...
    ├── MonoBleedingEdge/       # .NET Runtime
    └── UnityCrashHandler64.exe # Обработчик крашей
```

---

## ⚙️ Настройки билда

### Player Settings

Откройте: `Edit → Project Settings → Player`

**Рекомендуемые настройки:**

| Параметр | Значение |
|----------|----------|
| **Company Name** | `YourName` |
| **Product Name** | `Drag Racing` |
| **Version** | `1.0.0` |
| **Default Icon** | `icon.png` |
| **Fullscreen Mode** | `Fullscreen Window` |
| **Default Resolution** | `1920x1080` |
| **Run In Background** | `✓` |

**Иконка:**
1. Подготовьте PNG 512x512
2. `Player Settings → Icon`
3. Перетащите файл

---

### Build Settings

| Опция | Описание | Рекомендация |
|-------|----------|--------------|
| **Development Build** | Отладочная версия | ❌ Для релиза |
| **Compression Method** | LZ4 (быстрее) / LZ4HC (меньше) | LZ4HC |
| **Create Visual Studio Solution** | Для отладки | ❌ |
| **Copy PDB Files** | Символы отладки | ❌ Для релиза |

---

## 🔧 Устранение проблем

### ❌ "No scenes in build"

**Решение:**
```
File → Build Settings → Add Open Scenes
```
Добавьте все 6 сцен по порядку.

---

### ❌ "Build failed: Missing scripts"

**Причина:** Скрипты не скомпилированы

**Решение:**
1. Дождитесь компиляции в Unity (снизу справа "Compiling scripts...")
2. `Assets → Reimport All`

---

### ❌ "Out of disk space"

**Решение:**
- Освободите минимум 2 GB
- Очистите: `Library/` (Unity пересоздаст)
- Очистите: `Temp/`

---

### ❌ "Unity not found"

**Решение:**
```powershell
# Укажите правильный путь
.\unity-build.ps1 -unityPath "C:\Your\Path\Unity.exe"
```

---

## 📊 Размеры билда

| Платформа | Размер (сжатый) | Размер (распакованный) |
|-----------|-----------------|------------------------|
| Windows x64 | ~50 MB | ~150 MB |
| Windows x86 | ~45 MB | ~130 MB |

---

## 🚀 Публикация

### Steam

1. Создайте билд без отладки
2. Запакуйте в ZIP
3. Загрузите через Steamworks

### itch.io

1. Создайте билд
2. Запакуйте в ZIP
3. Загрузите на itch.io

### Прямая раздача

```bash
# Создайте ZIP архив
Compress-Archive -Path Builds\DragRacing-WinX64\* -DestinationPath DragRacing-v1.0.zip

# Или через 7-Zip
7z a -t7z -m0=lzma2 DragRacing-v1.0.7z Builds\DragRacing-WinX64\
```

---

## ✅ Чеклист перед сборкой

- [ ] Все сцены добавлены в Build Settings
- [ ] Сцены в правильном порядке (MainMenu = 0)
- [ ] Нет ошибок в консоли
- [ ] Все скрипты скомпилированы
- [ ] Player Settings настроены (название, версия, иконка)
- [ ] Тестовый запуск в Editor успешен
- [ ] Папка Builds существует

---

## 🎮 Запуск билда

```bash
# Из проводника
Дважды кликните на DragRacing.exe

# Из PowerShell
Start-Process "Builds\DragRacing-WinX64\DragRacing.exe"

# Из командной строки
cd Builds\DragRacing-WinX64
DragRacing.exe
```

---

## 📝 Логи сборки

| Файл | Описание |
|------|----------|
| `build-log.txt` | Лог Unity сборки |
| `Player.log` | Лог игры (AppData) |
| `Player-prev.log` | Предыдущий лог |

**Просмотр:**
```bash
# Текущий лог
notepad %USERPROFILE%\AppData\LocalLow\ProbMenu\DragRacing\Player.log

# Лог сборки
notepad build-log.txt
```

---

## 🛠️ Дополнительные команды

### Проверка сцен
```
Tools → Drag Racing → Build → Check Scenes
```

### Очистка билдов
```
Tools → Drag Racing → Build → Clean Build Folder
```

### Окно сборки
```
Tools → Drag Racing → Build → Build Window
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте `build-log.txt`
2. Проверьте консоль Unity
3. Перезапустите Unity
4. Очистите `Library/` и пересоберите

---

## ✅ Готово!

После успешной сборки:
- 📁 Билд в папке `Builds/`
- 🎮 Можно запускать `DragRacing.exe`
- 📦 Можно запаковать для раздачи

**Удачи в разработке!** 🏁
