# 🔧 ИСПРАВЛЕНИЕ ОШИБОК ДЛЯ UNITY 6 (6000.x.x)

## Проблема
Проект создан для **Unity 2021.3.15f1 LTS**. В **Unity 6000.3.9f1** изменились некоторые API.

## ✅ ЧТО УЖЕ ИСПРАВЛЕНО

Следующие файлы обновлены для совместимости с Unity 6:

1. **SaveManager.cs** - добавлен атрибут `[DefaultExecutionOrder]`
2. **GameManager.cs** - добавлен атрибут `[DefaultExecutionOrder]`
3. **InputManager.cs** - добавлен атрибут `[DefaultExecutionOrder]`

## 🔧 ДОПОЛНИТЕЛЬНЫЕ ИСПРАВЛЕНИЯ

### 1. Ошибка: "The type or namespace name 'DefaultExecutionOrder' could not be found"

**Решение:**
Этот атрибут доступен в Unity 2021.2+. В Unity 6 используйте:

```csharp
using UnityEngine;

[DefaultExecutionOrder(-100)] // Работает в Unity 6
public class MyClass : MonoBehaviour
```

**ИЛИ** удалите атрибуты `[DefaultExecutionOrder]` из всех файлов.

### 2. Ошибка: "SceneManager is not found"

**Решение:**
Добавьте namespace в файлы:

```csharp
using UnityEngine.SceneManagement;
```

**Файлы для исправления:**
- `GameInitializer.cs`
- `SetupChecker.cs`

### 3. Ошибка: "BuildPipeline does not exist"

**Решение:**
Добавьте namespace в Editor файлы:

```csharp
using UnityEditor;
```

**Файл:** `ScriptableObjectCreator.cs`

### 4. Ошибка компиляции в Safe Mode

**Решение:**

1. **Закройте Unity**
2. **Откройте файл:** `Assets/Scripts/Core/GameConfig.cs`
3. **Найдите и замените:**

```csharp
// Было:
[CreateAssetMenu(fileName = "GameConfig", menuName = "DragRace/Game Config")]

// Стало (оставьте как есть, это работает)
```

4. **Сохраните**

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Выйдите из Safe Mode

1. В диалоге выберите **Ignore**
2. Дождитесь импорта проекта

### Шаг 2: Проверьте консоль ошибок

1. **Window → General → Console**
2. Запишите номера ошибок

### Шаг 3: Исправьте ошибки

#### Ошибка CS0246: "The type or namespace name 'KeyBindings' could not be found"

**Файл:** `GameData.cs`

**Проблема:** Порядок компиляции

**Решение:**
1. Создайте папку `Assets/Scripts/Runtime`
2. Переместите туда:
   - `GameData.cs`
   - `VehicleParts.cs`
   - `CarDatabase.cs`
   - `CareerData.cs`

#### Ошибка CS0103: "The name 'Resources' does not exist in the current context"

**Решение:**
Добавьте `using UnityEngine;` в начало файла.

#### Ошибка: "JsonUtility does not exist"

**Решение:**
Добавьте `using UnityEngine;` в файлы сохранений.

### Шаг 4: Пересоберите проект

1. **Edit → Preferences → External Tools**
2. Нажмите **Regenerate project files**
3. Перезапустите Unity

## 🚀 БЫСТРОЕ ИСПРАВЛЕНИЕ (Автоматическое)

### Скрипт для авто-исправления:

Создайте файл `Assets/Scripts/Editor/FixUnity6Issues.cs`:

```csharp
using UnityEngine;
using UnityEditor;
using System.IO;

public class FixUnity6Issues : EditorWindow
{
    [MenuItem("Tools/Fix Unity 6 Issues")]
    public static void FixIssues()
    {
        Debug.Log("Исправление проблем Unity 6...");
        
        // Создание папки Runtime
        string runtimePath = "Assets/Scripts/Runtime";
        if (!Directory.Exists(runtimePath))
        {
            Directory.CreateDirectory(runtimePath);
            Debug.Log("Создана папка Runtime");
        }
        
        // Перемещение файлов данных
        string[] dataFiles = {
            "GameData.cs",
            "VehicleParts.cs", 
            "CarDatabase.cs",
            "CareerData.cs"
        };
        
        foreach (var file in dataFiles)
        {
            string oldPath = $"Assets/Scripts/Data/{file}";
            string newPath = $"{runtimePath}/{file}";
            
            if (File.Exists(oldPath))
            {
                File.Move(oldPath, newPath);
                Debug.Log($"Перемещён: {file}");
            }
        }
        
        AssetDatabase.Refresh();
        Debug.Log("Готово! Перезапустите Unity.");
    }
}
```

**Запустите:** **Tools → Fix Unity 6 Issues**

## ✅ ПРОВЕРКА ПОСЛЕ ИСПРАВЛЕНИЯ

1. **Откройте Console** (Ctrl+Shift+C)
2. Не должно быть ошибок (красных)
3. Предупреждения (жёлтые) - допустимы
4. Нажмите **Play** - игра должна запуститься

## 🐛 ЕСЛИ ОШИБКИ ОСТАЛИСЬ

### Вариант 1: Очистка кэша

1. Закройте Unity
2. Удалите папки:
   - `Library`
   - `Temp`
   - `Obj`
   - `Assembly-CSharp.csproj`
3. Откройте Unity заново

### Вариант 2: Откат к Unity 2021.3 LTS

1. Скачайте **Unity Hub**
2. Добавьте версию **2021.3.15f1 LTS**
3. Откройте проект в этой версии
4. **Идеально для совместимости!**

### Вариант 3: Создание нового проекта

1. Создайте новый проект в Unity 6
2. Скопируйте скрипты из старого проекта
3. Настройте сцены заново

## 📞 ПОДДЕРЖКА

### Логи для диагностики:

1. **Console → Open Console**
2. Скопируйте первые 10 ошибок
3. Проверьте:
   - Какие namespace не найдены?
   - Какие типы не существуют?

### Частые проблемы:

| Ошибка | Решение |
|--------|---------|
| `DefaultExecutionOrder` не найден | Удалить атрибут или обновить Unity |
| `JsonUtility` не найден | Добавить `using UnityEngine;` |
| `SceneManager` не найден | Добавить `using UnityEngine.SceneManagement;` |
| Круговая зависимость | Переместить файлы в Runtime папку |

## 🎯 РЕКОМЕНДАЦИЯ

**Для лучшей совместимости:**

1. **Идеально:** Unity 2021.3.15f1 LTS
2. **Хорошо:** Unity 2022.3.x LTS
3. **Работает:** Unity 6000.x.x (с исправлениями)

**Скачать Unity 2021.3 LTS:**
https://unity.com/releases/editor/whats-new/2021.3.15

---

## ✅ МИНИМАЛЬНЫЕ ИСПРАВЛЕНИЯ ДЛЯ ЗАПУСКА

1. **Удалите атрибуты** `[DefaultExecutionOrder]` из всех файлов
2. **Добавьте** `using UnityEngine;` в файлы с JsonUtility
3. **Перезапустите Unity**
4. **Нажмите Play**

**Игра должна запуститься!**
