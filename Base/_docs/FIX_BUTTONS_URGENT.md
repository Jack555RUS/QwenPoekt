# 🔧 СРОЧНАЯ НАСТРОЙКА КНОПОК В UNITY

**Проблема:** Текст не виден, клавиатура не работает  
**Решение:** Настроить в Unity Editor

---

## 🚀 ШАГИ ПО ИСПРАВЛЕНИЮ

### Шаг 1: Открыть Unity Editor

```
1. Откройте Unity Hub
2. Найдите проект: DragRaceUnity
3. Нажмите: Open
```

---

### Шаг 2: Открыть сцену MainMenu

```
1. В Project окне: Assets → Scenes → MainMenu
2. Дважды кликните на MainMenu.unity
3. Сцена откроется в редакторе
```

---

### Шаг 3: Проверить Canvas

**В Hierarchy найдите:**
```
Canvas
├── NewGameButton
│   └── Text (TMP)
├── ContinueButton
│   └── Text (TMP)
├── SaveButton
│   └── Text (TMP)
├── LoadButton
│   └── Text (TMP)
├── SettingsButton
│   └── Text (TMP)
└── ExitButton
    └── Text (TMP)
```

**Если нет Text (TMP) — создайте:**

```
1. Кликните правой кнопкой на кнопке
2. UI → Text - TextMeshPro
3. Переименуйте в "Text (TMP)"
```

---

### Шаг 4: Настроить TextMeshPro на кнопках

**Для каждой кнопки:**

1. **Выберите Text (TMP)** под кнопкой
2. **В Inspector найдите TextMeshPro компонент**
3. **Проверьте:**
   - ✅ Text: "НОВАЯ ИГРА" (или другая кнопка)
   - ✅ Font Size: 24
   - ✅ Color: Белый (R:255, G:255, B:255, A:255)
   - ✅ Alignment: Center

**Если шрифт розовый (Missing):**

```
1. Выберите Text (TMP)
2. В TextMeshPro компоненте:
3. Font Asset: Выберите любой доступный шрифт
4. Или: TMP Importer → Import TMP Essential Resources
```

---

### Шаг 5: Настроить цвета кнопок

**Для каждой кнопки:**

1. **Выберите кнопку** (NewGameButton и т.д.)
2. **В Inspector найдите Button компонент**
3. **Настройте Colors:**
   - Normal Color: Серый (R:128, G:128, B:128, A:255)
   - Highlighted Color: Светло-серый (R:160, G:160, B:160, A:255)
   - Pressed Color: Тёмный (R:100, G:100, B:100, A:255)
   - Selected Color: Ярко-жёлтый (R:255, G:255, B:0, A:255)

---

### Шаг 6: Настроить навигацию (Navigation)

**Для первой кнопки (NewGameButton):**

1. Выберите NewGameButton
2. В Button компоненте найдите **Navigation**
3. **Navigation Mode:** Automatic
4. **Select On Up:** ExitButton (зацикливание)
5. **Select On Down:** ContinueButton

**Для средних кнопок:**

```
ContinueButton:
- Select On Up: NewGameButton
- Select On Down: SaveButton

SaveButton:
- Select On Up: ContinueButton
- Select On Down: LoadButton

LoadButton:
- Select On Up: SaveButton
- Select On Down: SettingsButton

SettingsButton:
- Select On Up: LoadButton
- Select On Down: ExitButton
```

**Для последней кнопки (ExitButton):**

1. Выберите ExitButton
2. **Select On Up:** SettingsButton
3. **Select On Down:** NewGameButton (зацикливание)

---

### Шаг 7: Проверить EventSystem

**В Hierarchy найдите:**
```
EventSystem
```

**Если нет — создайте:**
```
1. GameObject → UI → Event System
2. EventSystem появится автоматически
```

**Проверьте компоненты на EventSystem:**
- ✅ EventSystem (скрипт)
- ✅ Standalone Input Module (скрипт)

---

### Шаг 8: Назначить MainMenuController

**В Hierarchy найдите:**
```
MainMenuManager
```

**Если нет — создайте:**
```
1. GameObject → Create Empty
2. Переименуйте в "MainMenuManager"
3. Перетащите скрипт MainMenuController на объект
```

**Настройте MainMenuController:**

1. Выберите MainMenuManager
2. В Inspector найдите MainMenuController скрипт
3. **Назначьте ссылки:**
   - Exit Dialog: Перетащите префаб ExitConfirmationDialog
   - First Button: NewGameButton
   - Last Button: ExitButton

---

### Шаг 9: Проверить onClick у кнопок

**Для каждой кнопки:**

1. Выберите кнопку (например, NewGameButton)
2. В Button компоненте найдите **On Click ()**
3. **Добавьте событие:**
   - Нажмите "+"
   - Перетащите MainMenuManager в поле объекта
   - Выберите функцию: MainMenuController → OnNewGame

**Повторите для всех кнопок:**

| Кнопка | Функция |
|--------|---------|
| NewGameButton | OnNewGame |
| ContinueButton | OnContinue |
| SaveButton | OnSave |
| LoadButton | OnLoad |
| SettingsButton | OnSettings |
| ExitButton | OnExit |

---

### Шаг 10: Сохранить и запустить

```
1. File → Save (Ctrl+S)
2. Нажмите Play (треугольник)
3. Проверьте:
   - ✅ Текст виден (белый на сером)
   - ✅ Клавиатура работает (↑↓ Enter)
   - ✅ Мышь работает (клик по кнопкам)
   - ✅ ВЫХОД открывает диалог
```

---

## 🐛 ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### 1. Текст розовый (Missing Font)

**Причина:** TMP шрифты не импортированы

**Решение:**
```
Window → TextMeshPro → Import TMP Essential Resources
Или выберите любой доступный шрифт в Font Asset
```

### 2. Клавиатура не работает

**Причина:** Нет EventSystem или Standalone Input Module

**Решение:**
```
1. Проверьте EventSystem в сцене
2. Добавьте Standalone Input Module на EventSystem
3. Проверьте Navigation у кнопок
```

### 3. Кнопки не нажимаются

**Причина:** Не назначены onClick события

**Решение:**
```
1. Проверьте On Click () у каждой кнопки
2. Назначьте MainMenuManager и функцию
```

### 4. Текст не виден

**Причина:** Белый текст на белом фоне

**Решение:**
```
1. Выберите Text (TMP)
2. Измените Color на белый
3. Проверьте цвет кнопки (должен быть серым)
```

---

## ✅ ЧЕК-ЛИСТ ГОТОВНОСТИ

- [ ] Все 6 кнопок видны
- [ ] Текст на кнопках белый, размер 24
- [ ] Фон кнопок серый
- [ ] EventSystem есть в сцене
- [ ] MainMenuManager есть в сцене
- [ ] Все onClick события назначены
- [ ] Navigation настроен (↑↓ работают)
- [ ] Enter активирует кнопку
- [ ] Escape открывает диалог выхода
- [ ] Сохранено (Ctrl+S)

---

**После настройки в Unity — протестируйте!** 🎮
