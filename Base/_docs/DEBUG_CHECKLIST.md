# ✅ DEBUGGING CHECKLIST — ЧЕК-ЛИСТ ОТЛАДКИ

**Версия:** 1.0  
**Дата:** 28 февраля 2026 г.  
**Проект:** DragRaceUnity

---

## 🔧 ПЕРЕД ОТЛАДКОЙ

### Окружение:

- [ ] Visual Studio 2022 установлен
- [ ] Компонент ".NET desktop development" установлен
- [ ] Unity 6000.3.10f1 установлен
- [ ] Проект открыт в Visual Studio
- [ ] Конфигурация установлена в **Debug**

### Подключение:

- [ ] Unity запущен
- [ ] Visual Studio запущен
- [ ] `Debug` → `Attach to Unity` выполнено
- [ ] Соединение установлено (значок отладки в Unity)

---

## 🎯 ТОЧКИ ОСТАНОВКИ

### Установить точки останова:

- [ ] `GameInitializer.Awake()` — инициализация
- [ ] `MainMenuController.OnNewGame()` — старт игры
- [ ] `MainMenuController.OnContinue()` — продолжение
- [ ] `SaveSystem.Save()` — сохранение
- [ ] `SaveSystem.Load()` — загрузка
- [ ] `Logger.Log()` — логирование

### Проверить точки:

- [ ] Точки активны (красный круг)
- [ ] Точки не серые (не пропущены)
- [ ] Точки в правильных файлах

---

## 👁️ ПЕРЕМЕННЫЕ ДЛЯ МОНИТОРИНГА

### PlayerData:

- [ ] `money` — деньги игрока
- [ ] `level` — уровень
- [ ] `experience` — опыт
- [ ] `currentCarId` — текущий автомобиль

### CarData:

- [ ] `maxSpeed` — максимальная скорость
- [ ] `acceleration` — ускорение
- [ ] `handling` — управление
- [ ] `weight` — вес

### Scene:

- [ ] `SceneManager.activeSceneCount` — количество сцен
- [ ] `SceneManager.GetActiveScene().name` — имя сцены

---

## 🖼️ ОКНА ОТЛАДКИ

### Открыть окна:

- [ ] **Autos** (`Ctrl+D, A`) — переменные текущей строки
- [ ] **Locals** (`Ctrl+D, L`) — локальные переменные
- [ ] **Watch** (`Ctrl+D, W`) — отслеживаемые переменные
- [ ] **Call Stack** (`Ctrl+D, C`) — стек вызовов
- [ ] **Immediate** (`Ctrl+D, I`) — немедленное выполнение

### Настроить Watch:

- [ ] Добавить `playerData.money`
- [ ] Добавить `playerData.level`
- [ ] Добавить `playerData.experience`
- [ ] Добавить `SceneManager.activeSceneCount`

---

## 🏃 ЗАПУСК ОТЛАДКИ

### Запуск:

- [ ] Нажать `F5` (Start Debugging)
- [ ] Unity перешёл в режим отладки
- [ ] Точки останова активны

### Тестирование:

- [ ] Нажать кнопку "Новая игра" в меню
- [ ] Проверить срабатывание точки останова
- [ ] Проверить переменные в Autos/Locals
- [ ] Проверить Call Stack

---

## 🐛 ПРОВЕРКА СИСТЕМ

### Главное меню:

- [ ] `MainMenuController.OnNewGame()` срабатывает
- [ ] `SceneManager.LoadScene()` вызывается
- [ ] Сцена загружается корректно

### Сохранения:

- [ ] `SaveSystem.Save()` записывает файл
- [ ] `SaveSystem.Load()` читает файл
- [ ] Данные сохраняются корректно

### Логирование:

- [ ] `Logger.Info()` выводит в консоль
- [ ] `Logger.Warning()` выводит предупреждения
- [ ] `Logger.Error()` выводит ошибки

---

## 🔍 ОТЛАДКА ПРОБЛЕМ

### Если точки останова не срабатывают:

- [ ] Проверить конфигурацию (Debug)
- [ ] Проверить подключение к Unity
- [ ] Пересобрать проект
- [ ] Перезапустить отладку

### Если переменные не отображаются:

- [ ] Проверить область видимости
- [ ] Использовать Watch окно
- [ ] Проверить тип переменной

### Если сохранение не работает:

- [ ] Проверить путь к файлу
- [ ] Проверить данные (playerData)
- [ ] Проверить исключения

---

## ✅ ПОСЛЕ ОТЛАДКИ

### Завершение:

- [ ] Остановить отладку (`Shift+F5`)
- [ ] Закрыть Visual Studio
- [ ] Закрыть Unity (если нужно)

### Коммит:

- [ ] Удалить временные точки останова
- [ ] Проверить изменения в коде
- [ ] Закоммитить изменения

---

## 📊 СТАТИСТИКА ОТЛАДКИ

| Дата | Проблема | Решение | Время |
|------|----------|---------|-------|
| 28.02.2026 | Первая отладка | Настройка окружения | 30 мин |

---

**Готово к отладке!** 🐛

**Последнее обновление:** 28 февраля 2026 г.
