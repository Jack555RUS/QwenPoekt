# Руководство по аудиту Базы Знаний

**Версия:** 1.0  
**Дата:** 2026-03-02  
**Статус:** ✅ Готово к использованию

---

## 📋 ОГЛАВЛЕНИЕ

1. [Назначение](#назначение)
2. [Принципы аудита](#принципы-аудита)
3. [Инструменты](#инструменты)
4. [Пошаговая инструкция](#пошаговая-инструкция)
5. [Типы проблем](#типы-проблем)
6. [Автоматизация](#автоматизация)
7. [Чек-лист](#чек-лист)

---

## 🎯 НАЗНАЧЕНИЕ

Аудит Базы Знаний (Ядра) — это процесс проверки качества, целостности и актуальности данных, используемых для обучения ИИ.

**Цели аудита:**

| Цель | Описание |
|------|----------|
| **Повышение точности** | Удаление нерелевантных и противоречивых данных |
| **Ускорение поиска** | Уменьшение объёма базы за счёт удаления мусора |
| **Снижение затрат** | Меньше токенов на обработку контекста |
| **Безопасность** | Удаление вредоносного или нежелательного контента |
| **Упрощение сопровождения** | Чистая структура и понятные имена файлов |

---

## 🔷 ПРИНЦИПЫ АУДИТА

### 1. Безопасность прежде всего

**Правило:** Никогда не работайте напрямую в боевой базе!

```powershell
# ✅ Правильно:
.\scripts\pre-operation-backup.ps1 -OperationType "Audit"
.\scripts\kb-audit.ps1
.\scripts\kb-cleanup.ps1 -WhatIf  # Сначала проверка
.\scripts\kb-cleanup.ps1 -Confirm  # Потом очистка

# ❌ Неправильно:
Remove-Item "KNOWLEDGE_BASE\*" -Recurse  # Без бэкапа!
```

---

### 2. Версионирование

**Правило:** Все изменения должны быть закоммичены в Git.

```powershell
# До аудита
git status
git commit -m "Before audit: YYYY-MM-DD"

# После аудита
git add .
git commit -m "Cleanup: аудит БЗ YYYY-MM-DD"
```

---

### 3. Постепенность

**Правило:** Не удаляйте всё сразу. Действуйте итеративно.

```
Аудит → Отчёт → Проверка → WhatIf → Confirm → Проверка → Коммит
```

---

### 4. Документирование

**Правило:** Каждое действие должно быть записано в OPERATION_LOG.md.

```markdown
## ГГГГ-ММ-ДД Аудит Базы Знаний

**Тип:** Аудит + Очистка
**Результаты:** Удалено XXX файлов, Архивировано XXX
**Статус:** ✅ Завершено
```

---

## 🛠️ ИНСТРУМЕНТЫ

### Основные скрипты

| Скрипт | Назначение | Команда |
|--------|------------|---------|
| **kb-audit.ps1** | Полный аудит БЗ | `.\scripts\kb-audit.ps1` |
| **kb-cleanup.ps1** | Очистка по результатам аудита | `.\scripts\kb-cleanup.ps1 -Confirm` |
| **pre-operation-backup.ps1** | Бэкап перед изменениями | `.\scripts\pre-operation-backup.ps1` |
| **check-hash-duplicates.ps1** | Проверка дубликатов | `.\scripts\check-hash-duplicates.ps1` |

### Вспомогательные утилиты

| Утилита | Назначение |
|---------|------------|
| **Get-FileHash** | Вычисление хэша файла (SHA256) |
| **Test-Path** | Проверка существования пути |
| **Get-ChildItem** | Получение списка файлов |

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Подготовка

```powershell
# 1. Проверить текущий статус Git
git status

# 2. Создать бэкап
.\scripts\pre-operation-backup.ps1 -OperationType "KB_Audit_$(Get-Date -Format 'yyyyMMdd')"

# 3. Закоммитить текущее состояние
git add .
git commit -m "Before audit: $(Get-Date -Format 'yyyy-MM-dd')"
```

---

### Шаг 2: Запуск аудита

```powershell
# Запустить аудит
.\scripts\kb-audit.ps1 -Path "D:\QwenPoekt\Base\KNOWLEDGE_BASE"

# Отчёт будет сохранён в:
# reports/KB_AUDIT_REPORT.md
```

**Что проверяет скрипт:**

- ✅ Файлы нулевого размера
- ✅ Временные файлы (.tmp, .bak, .DS_Store)
- ✅ Точные дубликаты (по хэшу SHA256)
- ✅ Битые файлы (не читаются)
- ✅ Нарушения именования (пробелы, кириллица, спецсимволы)
- ✅ Устаревшие версии (v1, v2, _old, _backup)

---

### Шаг 3: Анализ отчёта

Откройте `reports/KB_AUDIT_REPORT.md` и проверьте:

```markdown
## Статистика

| Метрика | Значение |
|---------|----------|
| **Всего файлов** | 800 |
| **Дубликатов** | 15 |
| **Битых файлов** | 3 |
| **Нарушений именования** | 25 |
```

**Вопросы для проверки:**

- [ ] Все ли дубликаты действительно лишние?
- [ ] Нет ли важных файлов среди битых?
- [ ] Правильно ли определены устаревшие версии?

---

### Шаг 4: Пробный запуск очистки

```powershell
# Режим WhatIf (проверка без удаления)
.\scripts\kb-cleanup.ps1 -AuditId "YYYYMMDD_HHMMSS" -WhatIf

# Скрипт покажет, что БУДЕТ удалено, но ничего не удалит
```

**Проверьте вывод:**

```
[WhatIf] Удалить: D:\...\file.tmp (Временный файл)
[WhatIf] Удалить: D:\...\duplicate.md (Дубликат)
[WhatIf] Переименовать: File Name.md → file_name.md
```

---

### Шаг 5: Реальная очистка

```powershell
# Только после проверки WhatIf!
.\scripts\kb-cleanup.ps1 -AuditId "YYYYMMDD_HHMMSS" -Confirm
```

**Что делает скрипт:**

1. Создаёт бэкап автоматически
2. Удаляет файлы нулевого размера
3. Удаляет временные файлы
4. Удаляет дубликаты (оставляет 1 копию)
5. Переименовывает файлы с нарушениями
6. Архивирует устаревшие версии в `04_ARCHIVES/`

---

### Шаг 6: Проверка результата

```powershell
# Запустить аудит ещё раз
.\scripts\kb-audit.ps1

# Сравнить с предыдущим отчётом
# Должно стать меньше проблем
```

---

### Шаг 7: Финальный коммит

```powershell
# Проверить изменения
git status

# Закоммитить
git add .
git commit -m "Cleanup: аудит БЗ YYYY-MM-DD (удалено XXX файлов)"

# Опционально: Push на GitHub
git push
```

---

## 🔍 ТИПЫ ПРОБЛЕМ

### 1. Дубликаты

**Что это:** Одинаковые файлы с одинаковым содержимым.

**Как найти:**
```powershell
.\scripts\check-hash-duplicates.ps1
```

**Что делать:**
- Оставить 1 копию (лучше в наиболее логичной папке)
- Остальные удалить или заменить симлинками

---

### 2. Битые файлы

**Что это:** Файлы, которые не открываются или повреждены.

**Как найти:**
```powershell
# Скрипт аудита проверяет читаемость
.\scripts\kb-audit.ps1
```

**Что делать:**
- Попытаться восстановить из бэкапа
- Если не удалось → удалить

---

### 3. Нарушения именования

**Что это:** Имена файлов не соответствуют правилам.

**Правила именования:**

| Правило | Пример |
|---------|--------|
| ✅ Латиница | `file_name.md` |
| ✅ Lower case | `file_name.md` |
| ✅ Подчёркивание | `file_name.md` |
| ❌ Пробелы | `file name.md` |
| ❌ Кириллица | `файл.md` |
| ❌ Спецсимволы | `file!@#.md` |

**Что делать:**
```powershell
# Автоматическое исправление
.\scripts\kb-cleanup.ps1 -Confirm
```

---

### 4. Файлы нулевого размера

**Что это:** Пустые файлы (0 байт).

**Как найти:**
```powershell
Get-ChildItem -Path "KNOWLEDGE_BASE" -Recurse -File | Where-Object { $_.Length -eq 0 }
```

**Что делать:** Удалить

---

### 5. Временные файлы

**Что это:** Остатки от редакторов, системные файлы.

**Расширения:**
- `.tmp`
- `.bak`
- `.DS_Store` (macOS)
- `.swp`, `.swo` (Vim)
- `~*` (временные файлы Office)

**Что делать:** Удалить

---

### 6. Устаревшие версии

**Что это:** Старые версии файлов (v1, v2, _old).

**Паттерны:**
- `*_v1.md`, `*_v2.md`
- `*_old.md`
- `*_backup.md`
- `*_2025-01-01.md`

**Что делать:**
- Актуальную версию оставить
- Старые → в `04_ARCHIVES/`

---

## 🤖 АВТОМАТИЗАЦИЯ

### Еженедельный аудит

**Скрипт:** `scripts\weekly-kb-audit.ps1` (будет создан)

```powershell
# Каждое воскресенье в 10:00
.\scripts\kb-audit.ps1

# Отчёт отправляется пользователю
# После утверждения → очистка
```

### Pre-commit хук

**Файл:** `.git/hooks/pre-commit`

```bash
#!/bin/bash

# Проверка перед коммитом
echo "Running KB audit check..."

# Проверка на файлы нулевого размера
ZERO_FILES=$(find KNOWLEDGE_BASE -type f -size 0)
if [ ! -z "$ZERO_FILES" ]; then
    echo "❌ Found zero-size files:"
    echo "$ZERO_FILES"
    exit 1
fi

# Проверка на временные файлы
TEMP_FILES=$(find KNOWLEDGE_BASE -name "*.tmp" -o -name "*.bak" -o -name ".DS_Store")
if [ ! -z "$TEMP_FILES" ]; then
    echo "❌ Found temp files:"
    echo "$TEMP_FILES"
    exit 1
fi

echo "✅ Audit check passed"
exit 0
```

---

## ✅ ЧЕК-ЛИСТ АУДИТА

### Перед аудитом

```markdown
[ ] 1. Git status чист
[ ] 2. Бэкап создан (pre-operation-backup.ps1)
[ ] 3. Отчёт предыдущего аудита просмотрен
```

### Во время аудита

```markdown
[ ] 1. kb-audit.ps1 выполнен
[ ] 2. Отчёт сохранён в reports/
[ ] 3. Запись в OPERATION_LOG.md
[ ] 4. WhatIf режим проверен
[ ] 5. Ручная проверка спорных файлов
```

### После аудита

```markdown
[ ] 1. kb-cleanup.ps1 выполнен
[ ] 2. Результат проверен (повторный аудит)
[ ] 3. Git коммит создан
[ ] 4. OPERATION_LOG.md обновлён
[ ] 5. Бэкап архивирован (если нужно)
```

---

## 📊 МЕТРИКИ КАЧЕСТВА

| Метрика | Цель | Формула |
|---------|------|---------|
| **% дубликатов** | < 1% | `(дубликаты / всего файлов) * 100` |
| **% битых файлов** | 0% | `(битые / всего файлов) * 100` |
| **% нарушений** | < 5% | `(нарушения / всего файлов) * 100` |
| **Чистый размер** | Минимизировать | `общий размер - размер мусора` |

---

## 🔗 СВЯЗАННЫЕ ФАЙЛЫ

| Файл | Описание |
|------|----------|
| [`DEDUP_GUIDE.md`](DEDUP_GUIDE.md) | Борьба с дубликатами |
| [`FILE_CHANGE_RULE.md`](FILE_CHANGE_RULE.md) | Правила именования |
| [`BACKUP_STRATEGY.md`](BACKUP_STRATEGY.md) | Стратегия бэкапа |
| [`LOG_GUIDE.md`](LOG_GUIDE.md) | Работа с логами |

---

**Версия:** 1.0  
**Дата:** 2026-03-02  
**Статус:** ✅ Готово к использованию

---

**Используйте это руководство для поддержания Базы Знаний в чистоте и порядке!** 🚀
