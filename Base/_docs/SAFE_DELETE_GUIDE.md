# Safe Delete — Безопасное удаление

**Назначение:** Удаление папок с автоматическим бэкапом и проверкой

---

## 🚀 БЫСТРЫЙ СТАРТ

### Перед удалением:

```powershell
# 1. Проверка (без удаления)
.\scripts\safe-delete.ps1 -Path "OLD/_INBOX/AbandonedProject" -WhatIf

# 2. Безопасное удаление (с бэкапом)
.\scripts\safe-delete.ps1 -Path "OLD/_INBOX/AbandonedProject"

# 3. Удаление без бэкапа (ОПАСНО!)
.\scripts\safe-delete.ps1 -Path "OLD/_INBOX/AbandonedProject" -NoBackup -Force
```

---

## 📋 ПАРАМЕТРЫ

| Параметр | Обязательный | Описание | Пример |
|----------|--------------|----------|--------|
| `-Path` | ✅ Да | Путь к папке для удаления | `"OLD/_INBOX/Project"` |
| `-BackupRoot` | ❌ Нет | Где хранить бэкапы | `"D:\QwenPoekt\_BACKUP"` |
| `-SourceRoot` | ❌ Нет | Корень проекта | `"D:\QwenPoekt\Base"` |
| `-WhatIf` | ❌ Нет | Только проверка (без удаления) | `-WhatIf` |
| `-NoBackup` | ❌ Нет | Удалить без бэкапа (ОПАСНО!) | `-NoBackup` |
| `-Force` | ❌ Нет | Принудительно (с -NoBackup) | `-Force` |

---

## 📖 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Безопасное удаление с бэкапом

```powershell
# Шаг 1: Проверка
.\scripts\safe-delete.ps1 -Path "OLD/_INBOX/AbandonedProject" -WhatIf

# Вывод:
#   Файлов: 15
#   Размер: 0.5 MB
#   Бэкап: Будет создан

# Шаг 2: Удаление с бэкапом
.\scripts\safe-delete.ps1 -Path "OLD/_INBOX/AbandonedProject"

# Вывод:
# ✅ Бэкап создан: D:\QwenPoekt\_BACKUP\2026-03-02_03-30_Delete_AbandonedProject
# ✅ Целостность подтверждена: 15 файлов
# ✅ Папка удалена: OLD/_INBOX/AbandonedProject
```

### Пример 2: Удаление без бэкапа (ОПАСНО!)

```powershell
# ТОЛЬКО если уверены на 100%!
.\scripts\safe-delete.ps1 -Path "_TEMP/Cache" -NoBackup -Force

# ⚠️ WARNING: Бэкап НЕ создан!
# ⚠️ WARNING: Восстановление невозможно!
```

### Пример 3: Проверка критичной папки

```powershell
# Попытка удалить критичную папку
.\scripts\safe-delete.ps1 -Path "KNOWLEDGE_BASE"

# Вывод:
# ❌ ОШИБКА: КРИТИЧНАЯ ПАПКА! Удаление запрещено!
#   Критичные папки: KNOWLEDGE_BASE, scripts, reports, .qwen, ...
```

---

## 🔍 ЧТО ДЕЛАЕТ СКРИПТ

```
1. Проверка пути
   ├─ Существует ли папка?
   ├─ Количество файлов
   └─ Размер в MB

2. Проверка на критичность
   ├─ KNOWLEDGE_BASE? → ❌ ЗАПРЕЩЕНО
   ├─ scripts? → ❌ ЗАПРЕЩЕНО
   ├─ reports? → ❌ ЗАПРЕЩЕНО
   ├─ .qwen? → ❌ ЗАПРЕЩЕНО
   └─ Другая? → ✅ МОЖНО

3. WhatIf режим (опционально)
   └─ Показать информацию без удаления

4. Бэкап (если не -NoBackup)
   ├─ Копирование в _BACKUP
   ├─ Проверка целостности
   └─ Запись в OPERATION_LOG.md

5. Удаление
   ├─ Remove-Item -Recurse -Force
   └─ Проверка: папка удалена?

6. Запись в журнал
   └─ reports/OPERATION_LOG.md
```

---

## 🛡️ ЗАЩИТА ОТ ОШИБОК

### Критичные папки (ЗАПРЕЩЕНО):

```
❌ KNOWLEDGE_BASE      # База знаний (740 файлов)
❌ scripts              # Скрипты автоматизации
❌ reports              # Отчёты и логи
❌ .qwen                # Конфиг ИИ
❌ _templates           # Шаблоны
❌ _docs                # Документация
❌ PROJECTS             # Unity проекты
❌ .vscode              # Настройки VS Code
❌ .github              # GitHub конфиги
```

### Критичные файлы (ЗАПРЕЩЕНО):

```
❌ AI_START_HERE.md
❌ QWEN.md
❌ RULES_AND_TASKS.md
❌ NOTES.md
❌ ТЕКУЩАЯ_ЗАДАЧА.md
```

**Если попытаться удалить критичную папку:**

```
❌ ОШИБКА: КРИТИЧНАЯ ПАПКА! Удаление запрещено!

Если вы уверены, используйте -Force (опасно!)
```

---

## 📊 ЖИЗНЕННЫЙ ЦИКЛ УДАЛЕНИЯ

```
ДЕНЬ 0: Удаление
└─ safe-delete.ps1 → Бэкап → Удаление → Лог

ДЕНЬ 1-45: Хранение бэкапа
└─ Бэкап доступен для восстановления

ДЕНЬ 45+: Глубокий анализ
└─ old-backup-analysis.ps1 (будет создан)
   ├─ Что изменилось?
   ├─ Есть ли ценность?
   └─ Рекомендация: удалить/сохранить

ДЕНЬ 60: Удаление бэкапа
└─ old-backup-cleanup.ps1 (будет создан)
```

---

## 🔄 ВОССТАНОВЛЕНИЕ ПОСЛЕ УДАЛЕНИЯ

```powershell
# Если нужно восстановить удалённую папку:

Copy-Item "D:\QwenPoekt\_BACKUP\2026-03-02_03-30_Delete_AbandonedProject" `
          "D:\QwenPoekt\Base\OLD\_INBOX\AbandonedProject" `
          -Recurse -Force
```

---

## ⚠️ ВАЖНЫЕ ПРАВИЛА

1. **ВСЕГДА проверяй путь** перед удалением (`-WhatIf`)
2. **НЕ используй -NoBackup** без крайней необходимости
3. **НЕ используй -Force** без проверки
4. **Запомни путь к бэкапу** (выводится после удаления)
5. **Проверяй OPERATION_LOG.md** после удаления

---

## 📋 ЧЕК-ЛИСТ ПЕРЕД УДАЛЕНИЕМ

```
[ ] 1. Запустить с -WhatIf (проверка)
[ ] 2. Проверить: "Папка не критична"
[ ] 3. Проверить количество файлов
[ ] 4. Запомнить путь к бэкапу (будет выведен)
[ ] 5. Запустить без -WhatIf (удаление)
[ ] 6. Проверить: "✅ УДАЛЕНИЕ ЗАВЕРШЕНО!"
[ ] 7. Проверить OPERATION_LOG.md
```

---

## 🎯 СВЯЗАННЫЕ СКРИПТЫ

| Скрипт | Назначение |
|--------|------------|
| `pre-operation-backup.ps1` | Бэкап перед операцией |
| `safe-delete.ps1` | Безопасное удаление (этот скрипт) |
| `old-backup-analysis.ps1` | Анализ старых бэкапов (>45 дней, будет создан) |
| `auto-commit-daily.ps1` | Ежедневный Git коммит (18:00) |

---

## 🧠 ОТЛИЧИЯ ОТ Remove-Item

| Функция | Remove-Item | safe-delete.ps1 |
|---------|-------------|-----------------|
| Бэкап | ❌ Нет | ✅ Автоматически |
| Проверка целостности | ❌ Нет | ✅ Сравнение файлов |
| Проверка критичности | ❌ Нет | ✅ Защита от удаления важного |
| Логирование | ❌ Нет | ✅ Запись в OPERATION_LOG.md |
| WhatIf режим | ✅ Есть | ✅ Есть (расширенный) |
| Восстановление | ❌ Сложно | ✅ Путь к бэкапу в выводе |

---

**Версия:** 1.0  
**Дата:** 2026-03-02  
**Статус:** ✅ Готов к использованию
