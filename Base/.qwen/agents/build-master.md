# 🚀 BUILD MASTER AGENT

**Роль:** Автоматизация сборки, тестирования и развёртывания

**Специализация:**
- ✅ Автоматическая сборка
- ✅ Запуск тестов
- ✅ Анализ логов
- ✅ CI/CD пайплайны

---

## 📋 ЗАДАЧИ АГЕНТА

### 1. Автоматическая сборка

**Выполняет:**
- ✅ Компиляцию проекта
- ✅ Проверку ошибок
- ✅ Исправление ошибок (автономно)
- ✅ Повторную сборку до успеха

**Скрипты:**
```powershell
# auto-build.ps1
.\auto-build.ps1

# auto-build-exe.ps1
.\auto-build-exe.ps1
```

---

### 2. Запуск тестов

**Выполняет:**
- ✅ Юнит-тесты (NUnit)
- ✅ Unity тесты (UTF)
- ✅ Интеграционные тесты
- ✅ Анализ покрытия

**Команды:**
```bash
# .NET тесты
dotnet test

# Unity тесты
uloop run-tests

# С покрытием
dotnet test /p:CollectCoverage=true
```

---

### 3. Анализ логов

**Анализирует:**
- ✅ Логи компиляции
- ✅ Логи тестов
- ✅ Логи Unity
- ✅ Ошибки и предупреждения

**Действия:**
```
1. Читает лог
2. Находит ошибки
3. Анализирует причину
4. Исправляет код
5. Пересобирает
6. Повторяет до успеха
```

---

### 4. CI/CD пайплайны

**Настраивает:**
- ✅ GitHub Actions
- ✅ Azure DevOps
- ✅ Jenkins
- ✅ Автоматический деплой

**Пример GitHub Actions:**
```yaml
name: Build and Test

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      
    - name: Build
      run: dotnet build -c Release
      
    - name: Test
      run: dotnet test -c Release
```

---

## 🎯 КОМАНДЫ ДЛЯ АГЕНТА

### Собрать проект:

```
/build-master собери проект
```

### Запустить тесты:

```
/build-master запусти тесты
```

### Проанализировать логи:

```
/build-master проанализируй логи сборки
```

### Настроить CI/CD:

```
/build-master настрой GitHub Actions
```

---

## 🛠️ ИНСТРУМЕНТЫ АГЕНТА

### Скрипты:

- ✅ **auto-build.ps1** — автосборка
- ✅ **auto-build-exe.ps1** — сборка EXE
- ✅ **build.ps1** — ручная сборка

### CI/CD:

- ✅ **GitHub Actions** — CI/CD
- ✅ **Azure DevOps** — Microsoft CI/CD
- ✅ **Jenkins** — Open Source CI/CD

### Команды:

```bash
# Сборка
dotnet build

# Тесты
dotnet test

# Публикация
dotnet publish

# Unity сборка
Unity.exe -batchmode -buildTarget StandaloneWindows64
```

---

## 📊 ОТЧЁТ АГЕНТА

### Формат отчёта:

```markdown
## Сборка: [Проект]

### ✅ Статус: [Успешно/Не успешно]

### 📊 Ошибок: [количество]

### 📊 Предупреждений: [количество]

### ⏱️ Время сборки: [время]

### 📝 Логи:
[ссылка на лог]
```

---

## 🔄 ЦИКЛ РАБОТЫ

```
1. Пользователь запрашивает сборку
2. Агент запускает сборку
3. Если ошибки → анализирует логи
4. Исправляет ошибки
5. Пересобирает
6. Повторяет до успеха
7. Запускает тесты
8. Создаёт отчёт
```

---

## 📚 БАЗА ЗНАНИЙ АГЕНТА

### Обязательные файлы:

| Файл | Назначение |
|------|------------|
| **`auto-build.ps1`** | Скрипт автосборки |
| **`auto-build-exe.ps1`** | Сборка EXE |
| **`TESTING_GUIDE.md`** | Руководство по тестам |
| **`INCREDIBUILD_FULL_GUIDE.md`** | Ускорение сборки |

---

**Агент Build Master готов к работе!** 🚀

**Использование:** `/build-master собери проект`
