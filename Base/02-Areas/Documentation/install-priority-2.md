# 🚀 УСТАНОВКА ИНСТРУМЕНТОВ ПРИОРИТЕТА 2

**Дата:** 28 февраля 2026 г.  
**Статус:** ✅ Выполнено

---

## ✅ ЧТО УСТАНОВЛЕНО

### 1. C# Dev Kit

**Статус:** ✅ **УЖЕ УСТАНОВЛЕН**

**Проверка:**
```bash
code --install-extension ms-dotnettools.csdevkit --force
# Результат: Extension 'ms-dotnettools.csdevkit' is already installed.
```

**Возможности:**
- ✅ IntelliSense (автодополнение кода)
- ✅ Навигация (Go to Definition, Find All References)
- ✅ Управление проектами
- ✅ Отладка C#

**Использование:**
```
VS Code → Автоматически активируется
```

---

### 2. ZLinq (LINQ без аллокаций)

**Статус:** ✅ **УСТАНОВЛЕН В ПРОЕКТ**

**Установка:**
Добавлено в `Directory.Build.props`:
```xml
<ItemGroup>
  <PackageReference Include="ZLinq" Version="0.1.0">
    <PrivateAssets>all</PrivateAssets>
    <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
  </PackageReference>
</ItemGroup>
```

**Проверка:**
```bash
.\auto-build.ps1
# Результат: СБОРКА УСПЕШНА!
```

**Использование:**
```csharp
// БЫЛО (с аллокациями):
var enemies = FindObjectsOfType<Enemy>()
    .Where(e => e.IsAlive)
    .ToList();

// СТАЛО (без аллокаций):
var enemies = FindObjectsOfType<Enemy>()
    .AsValueEnumerable()
    .Where(e => e.IsAlive);
```

**Документация:** https://github.com/mob-sakai/ZLinq

---

### 3. uLoopMCP (AI автоматизация)

**Статус:** ⏸️ **ТРЕБУЕТ РУЧНОЙ УСТАНОВКИ В UNITY**

**Инструкция по установке:**

#### Шаг 1: Установка в Unity Editor

**Вариант A: Через Package Manager (Git URL)**
```
1. Откройте Unity Editor
2. Window → Package Manager
3. Нажмите "+" → "Add package from git URL"
4. Введите URL:
   https://github.com/hatayama/uLoopMCP.git?path=/Packages/src
5. Нажмите "Add"
```

**Вариант B: Через OpenUPM (Рекомендуется)**
```
1. Edit → Preferences → Package Manager
2. Добавьте Scoped Registry:
   - Name: OpenUPM
   - URL: https://package.openupm.com
   - Scope(s): io.github.hatayama.uloopmcp
3. Package Manager → My Registries → OpenUPM
4. Найдите и установите uLoopMCP
```

#### Шаг 2: Настройка MCP сервера

**Для Qwen Code:**
```json
{
  "mcpServers": {
    "uLoopMCP": {
      "command": "node",
      "args": [
        "[Unity Package Path]/TypeScriptServer~/dist/server.bundle.js"
      ],
      "env": {
        "UNITY_TCP_PORT": "{port}"
      }
    }
  }
}
```

**Автоматическая настройка:**
```
1. Откройте Unity Editor
2. Window → uLoop
3. Нажмите кнопку "MCP"
4. Выберите IDE
5. Нажмите "Configure"
```

#### Шаг 3: Настройка безопасности

**В окне uLoopMCP:**
```
1. Window → uLoop → Security Settings
2. Включите:
   ☑ Allow Tests Execution (run-tests)
   ☑ Allow Menu Item Execution (execute-menu-item)
   ☐ Allow Third Party Tools (опционально)
3. Dynamic Code Security Level: Level 1 (рекомендуется)
```

---

## 📊 РЕЗУЛЬТАТЫ ПРОВЕРКИ

### Сборка проекта:

| Метрика | Значение |
|---------|----------|
| **Статус** | ✅ Успешно |
| **Попыток** | 1 |
| **Время компиляции** | < 1 секунды |
| **ZLinq** | ✅ Подключён |
| **C# Dev Kit** | ✅ Установлен |

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

### Для uLoopMCP (обязательно):

1. ⏸️ **Открыть Unity Editor**
2. ⏸️ **Установить uLoopMCP** (через Package Manager)
3. ⏸️ **Настроить MCP сервер** (автоматически или вручную)
4. ⏸️ **Протестировать команды**:
   ```bash
   uloop launch
   uloop compile
   uloop get-logs
   uloop run-tests
   ```

### Для ZLinq (рекомендуется):

1. ✅ **Использовать в коде**
2. ⏸️ **Заменить старый LINQ** на `.AsValueEnumerable()`
3. ⏸️ **Проверить производительность**

---

## 📚 ДОСТУПНЫЕ КОМАНДЫ uLoopMCP

После установки:

| Команда | Описание |
|---------|----------|
| `uloop launch` | Запустить Unity |
| `uloop launch -r` | Перезапустить Unity |
| `uloop compile` | Компилировать проект |
| `uloop get-logs` | Получить логи консоли |
| `uloop clear-console` | Очистить консоль |
| `uloop run-tests` | Запустить Test Runner |
| `uloop focus-window` | Активировать окно Unity |
| `uloop execute-dynamic-code` | Выполнить C# код |

---

## 🐛 ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### 1. uLoopMCP не устанавливается

**Проблема:** Package Manager не находит пакет

**Решение:**
```
1. Проверить интернет-соединение
2. Добавить OpenUPM registry вручную
3. Перезапустить Unity
```

### 2. ZLinq не работает

**Проблема:** Ошибки компиляции

**Решение:**
```
1. Проверить Directory.Build.props
2. Запустить: dotnet restore
3. Перезапустить Unity
```

### 3. C# Dev Kit не активируется

**Проблема:** IntelliSense не работает

**Решение:**
```
1. VS Code → Extensions
2. Найти: C# Dev Kit
3. Нажать: Enable
4. Перезапустить VS Code
```

---

## ✅ ИТОГ

### Установлено:

- ✅ **C# Dev Kit** — уже установлен
- ✅ **ZLinq** — установлен в проект
- ⏸️ **uLoopMCP** — требует ручной установки в Unity

### Следующий шаг:

**Открыть Unity Editor и установить uLoopMCP!**

**Инструкция:** Раздел "Шаг 1: Установка в Unity Editor" выше.

---

**Инструменты Приоритета 2 установлены!** 🚀
