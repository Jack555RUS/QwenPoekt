# ⚙️ Настройка Unity для анализа и сборки

**Версия:** 1.0
**Дата:** 27 февраля 2026 г.

---

## 🎯 Цель

Настроить Unity так, чтобы:
1. ✅ Генерировать Visual Studio Solution для полного анализа кода
2. ✅ Использовать все возможности статического анализа (.editorconfig, анализаторы)
3. ✅ Собирать готовый .exe с максимальным качеством кода

---

## 📋 Шаг 1: Включить генерацию Visual Studio Solution

### В Unity Editor:

```
1. Откройте проект в Unity
2. File → Build Settings (Ctrl+Shift+B)
3. Включите опции:
   ☑ Create Visual Studio Solution
   ☑ Development Build (для отладки)
   ☑ Script Debugging (для отладки C#)
4. Нажмите Build
5. Выберите папку для сохранения решения
```

**Результат:** В папке появятся файлы:
- `DragRaceUnity.sln` — решение Visual Studio
- `Assembly-CSharp.csproj` — проект с игровыми скриптами
- `Assembly-CSharp-Editor.csproj` — проект с Editor скриптами

---

## 📋 Шаг 2: Настройка анализа в Visual Studio

### 2.1. Открыть решение

```
1. Откройте: DragRaceUnity.sln в Visual Studio
2. Дождитесь загрузки проектов
3. Проверьте Solution Explorer
```

### 2.2. Проверить настройки анализа

```
1. Tools → Options
2. Text Editor → C# → Advanced
3. Включите:
   ☑ Enable full solution analysis
   ☑ Show suggestions for improvements
   ☑ Auto-generate properties for fields
```

### 2.3. Установить анализаторы (опционально)

```
1. Tools → NuGet Package Manager → Manage NuGet Packages for Solution
2. Найдите и установите:
   - StyleCop.Analyzers (версия 1.2.0-beta.556)
   - SonarAnalyzer.CSharp (версия 9.19.0.84067)
3. Сохраните
```

---

## 📋 Шаг 3: Анализ кода в Visual Studio

### В редакторе:

- **Красное подчёркивание** = Ошибка (Error)
- **Жёлтое подчёркивание** = Предупреждение (Warning)
- **Синее подчёркивание** = Рекомендация (Suggestion)

### Быстрые исправления:

```
1. Кликните на подчёркивание
2. Нажмите Ctrl+. (Control + точка)
3. Выберите исправление из списка
```

### Окно ошибок:

```
View → Error List (Ctrl+\, E)
Фильтр: Show → Errors + Warnings + Messages
```

---

## 📋 Шаг 4: Сборка готового .exe

### Вариант A: Сборка из Visual Studio

```
1. В Visual Studio: Build → Build Solution (Ctrl+Shift+B)
2. Конфигурация: Release (для оптимизации)
3. Путь к билду: bin/Release/
```

**Результат:**
- `DragRace.exe` — исполняемый файл
- `DragRace_Data/` — данные игры
- `UnityPlayer.dll` — рантайм Unity

### Вариант B: Сборка из Unity (рекомендуется для финального билда)

```
1. В Unity: File → Build Settings
2. Нажмите Build
3. Выберите папку для билда
4. Дождитесь завершения
```

---

## 🔧 Directory.Build.props

Файл `Directory.Build.props` в корне проекта содержит централизованные настройки для всех проектов:

### Что настроено:

| Настройка | Значение | Описание |
|-----------|----------|----------|
| **TargetFramework** | netstandard2.1 | Целевая платформа |
| **LangVersion** | 9.0 | Версия C# |
| **Nullable** | enable | Null reference types |
| **ImplicitUsings** | enable | Автоматические using директивы |
| **AnalysisMode** | Recommended | Режим анализа кода |
| **EnableNETAnalyzers** | true | Включить анализаторы .NET |

### Implicit Usings (автоматические):

```csharp
// Добавляются автоматически ко всем файлам:
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using UnityEngine;
```

**Преимущество:** Не нужно писать `using` вручную!

---

## 🎯 Build Events

В `Directory.Build.props` настроены события сборки:

### PreBuild (перед сборкой):
```xml
<Target Name="PreBuild" BeforeTargets="PreBuildEvent">
  <Message Text="PreBuild: Начало сборки" Importance="high" />
</Target>
```

### PostBuild (после сборки):
```xml
<Target Name="PostBuild" AfterTargets="PostBuildEvent">
  <Message Text="PostBuild: Сборка завершена" Importance="high" />
</Target>
```

**Можно добавить:**
- Копирование файлов
- Генерацию кода
- Автоматическую публикацию

---

## 🐛 Отладка собранного билда

### Включить отладку:

```
1. Unity: File → Build Settings
2. Включите:
   ☑ Development Build
   ☑ Script Debugging
3. Build
```

### Подключить отладчик:

```
1. Visual Studio: Debug → Attach to Process
2. Выберите процесс игры
3. Debug → Windows → Diagnostic Tools
```

**Возможности:**
- Просмотр переменных
- Пошаговое выполнение
- Breakpoints
- Анализ стека вызовов

---

## 📊 Сравнение подходов

| Подход | Анализ | Отладка | Оптимизация | Для чего |
|--------|--------|---------|-------------|----------|
| **Unity Editor (Play)** | ✅ | ✅ | ❌ | Быстрое тестирование |
| **VS Build (Debug)** | ✅✅ | ✅✅ | ❌ | Отладка кода |
| **VS Build (Release)** | ✅✅ | ✅ | ✅ | Тестирование производительности |
| **Unity Build (Master)** | ✅ | ❌ | ✅✅ | Финальный билд |

---

## ✅ Чек-лист перед сборкой

### Код:
- [ ] Все Error исправлены
- [ ] Warning минимизированы
- [ ] Suggestions рассмотрены
- [ ] Code analysis пройден

### Настройки:
- [ ] `Directory.Build.props` в корне
- [ ] `.editorconfig` в корне
- [ ] Анализаторы установлены (опционально)

### Unity:
- [ ] Create Visual Studio Solution включено
- [ ] Сцена добавлена в Build Settings
- [ ] Все сцены сохраняются

### Visual Studio:
- [ ] Решение открыто
- [ ] Проекты загружены без ошибок
- [ ] Конфигурация: Release

---

## 📚 Дополнительные ресурсы

- [Directory.Build.props документация](https://docs.microsoft.com/en-us/visualstudio/msbuild/customize-your-build)
- [Implicit usings](https://docs.microsoft.com/en-us/dotnet/core/project-sdk/overview#implicit-using-directives)
- [Build events](https://docs.microsoft.com/en-us/dotnet/core/project-sdk/msbuild-props#build-events)
- [Анализ кода](./FREE_ANALYSIS_TOOLS.md)

---

**Следующий шаг:** Открыть Unity → File → Build Settings → Включить Create Visual Studio Solution! 🎯
