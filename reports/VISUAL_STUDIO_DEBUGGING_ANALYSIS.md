# 🐛 VISUAL STUDIO DEBUGGING GUIDE — АНАЛИЗ И РЕКОМЕНДАЦИИ

**Дата:** 28 февраля 2026 г.  
**Статус:** ✅ Проанализировано  
**Источник:** Microsoft Docs — Visual Studio Debugging

---

## 📊 КРАТКОЕ СОДЕРЖАНИЕ

**Руководство описывает:**
1. ✅ Запуск отладчика и точки останова
2. ✅ Пошаговое выполнение кода (F10, F11)
3. ✅ Инспекция переменных (Data Tips, Autos, Locals)
4. ✅ Окно Watch (часы)
5. ✅ Стек вызовов (Call Stack)
6. ✅ Run to Click (Ctrl+F10)
7. ✅ Перезапуск отладки (Ctrl+Shift+F5)

---

## 🎯 ПРИМЕНИМОСТЬ К НАШЕМУ ПРОЕКТУ

### ✅ Высокая применимость:

| Функция | Применение в проекте |
|---------|---------------------|
| **Точки останова** | Отладка `MainMenuController.cs`, `SaveSystem.cs` |
| **Пошаговое выполнение** | Анализ `GameInitializer.cs`, `Logger.cs` |
| **Инспекция переменных** | Проверка `PlayerData`, `CarData` |
| **Стек вызовов** | Отладка EventBus, SaveSystem |
| **Watch окно** | Мониторинг здоровья, денег игрока |

### ⚠️ Средняя применимость:

| Функция | Применение |
|---------|------------|
| **Run to Click** | Быстрый переход к коду в больших файлах |
| **Перезапуск отладки** | Тестирование инициализации проекта |

---

## 📋 РЕКОМЕНДАЦИИ ПО УЛУЧШЕНИЮ

### 1. Создать руководство по отладке для проекта

**Файл:** `PROJECTS/DragRaceUnity/DEBUGGING_GUIDE.md`

**Содержание:**
- Как настроить отладку в Unity
- Точки останова для основных систем
- Частые проблемы и решения

---

### 2. Добавить отладочные скрипты

**Скрипты:**

#### `scripts/debug-unity.ps1`
```powershell
# Запуск Unity в режиме отладки
Start-Process "C:\Program Files\Unity\Hub\Editor\6000.3.10f1\Editor\Unity.exe" `
    -ArgumentList "-projectPath", "D:\QwenPoekt\PROJECTS\DragRaceUnity", "-debugMode"
```

#### `scripts/debug-vs.ps1`
```powershell
# Открытие решения в Visual Studio с отладкой
Start-Process "devenv.exe" `
    -ArgumentList "D:\QwenPoekt\PROJECTS\DragRaceUnity\DragRaceUnity.sln", "/debug"
```

---

### 3. Создать конфигурацию отладки

**Файл:** `PROJECTS/DragRaceUnity/.vscode/launch.json`

```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Unity Attach",
            "type": "unity",
            "request": "attach"
        },
        {
            "name": "Unity Play Mode",
            "type": "unity",
            "request": "launch"
        }
    ]
}
```

---

### 4. Добавить отладочные точки в код

**Примеры:**

#### `MainMenuController.cs`
```csharp
public void OnNewGame()
{
    // 🔴 BREAKPOINT: Проверка инициализации новой игры
    Logger.Info("[MainMenu] Новая игра начата");
    
    // 🔴 WATCH: SceneManager.activeSceneCount
    SceneManager.LoadScene("GameScene");
}
```

#### `SaveSystem.cs`
```csharp
public void Save(int slot)
{
    // 🔴 BREAKPOINT: Проверка данных перед сохранением
    // 🔴 WATCH: playerData.money, playerData.level
    string json = JsonUtility.ToJson(playerData);
    File.WriteAllText(path, json);
}
```

---

### 5. Создать чек-лист отладки

**Файл:** `PROJECTS/DragRaceUnity/DEBUG_CHECKLIST.md`

**Чек-лист:**

#### Перед отладкой:
- [ ] Установить Visual Studio 2022
- [ ] Установить компонент ".NET desktop development"
- [ ] Открыть решение в Visual Studio
- [ ] Подключить отладчик к Unity

#### Точки останова:
- [ ] `GameInitializer.Awake()` — инициализация
- [ ] `MainMenuController.OnNewGame()` — старт игры
- [ ] `SaveSystem.Save()` — сохранение
- [ ] `SaveSystem.Load()` — загрузка
- [ ] `Logger.Log()` — логирование

#### Переменные для мониторинга:
- [ ] `PlayerData.money` — деньги игрока
- [ ] `PlayerData.level` — уровень
- [ ] `PlayerData.experience` — опыт
- [ ] `CarData.maxSpeed` — скорость автомобиля
- [ ] `CarData.acceleration` — ускорение

---

### 6. Интеграция с существующими скриптами

**Обновить:** `scripts/auto-commit-knowledge.ps1`

**Добавить:**
```powershell
# Проверка перед коммитом
Write-Host "🐛 Проверка отладочных файлов..." -ForegroundColor Yellow

$debugFiles = Get-ChildItem "PROJECTS" -Recurse -Filter "*.pdb"
if ($debugFiles.Count -gt 0) {
    Write-Host "  ⚠️  Найдены отладочные файлы (.pdb)" -ForegroundColor Yellow
    Write-Host "  Рекомендуется очистить перед коммитом" -ForegroundColor Gray
}
```

---

### 7. Создать базу знаний по отладке

**Файл:** `KNOWLEDGE_BASE/02_UNITY/UNITY_DEBUGGING_GUIDE.md`

**Структура:**
1. **Введение**
   - Что такое отладка
   - Зачем нужна отладка в Unity

2. **Настройка окружения**
   - Установка Visual Studio
   - Настройка подключения к Unity
   - Конфигурация VS Code

3. **Основные техники**
   - Точки останова
   - Пошаговое выполнение
   - Инспекция переменных

4. **Продвинутые техники**
   - Условные точки останова
   - Логирование в отладке
   - Профилирование

5. **Частые проблемы**
   - Отладчик не подключается
   - Точки останова не срабатывают
   - Переменные не отображаются

6. **Best Practices**
   - Где ставить точки останова
   - Какие переменные мониторить
   - Когда использовать логирование

---

## 🔧 ПРАКТИЧЕСКИЕ ПРИМЕРЫ

### Пример 1: Отладка сохранения

**Проблема:** Сохранение не работает

**Решение:**
1. Поставить точку останова в `SaveSystem.Save()`
2. Запустить отладку (F5)
3. Проверить переменную `playerData` в окне Locals
4. Проверить путь к файлу в окне Watch
5. Пошагово выполнить (F10) запись файла

---

### Пример 2: Отладка меню

**Проблема:** Кнопка не работает

**Решение:**
1. Поставить точку останова в `MainMenuController.OnNewGame()`
2. Нажать кнопку в Unity
3. Проверить, срабатывает ли точка останова
4. Проверить стек вызовов (Call Stack)
5. Проверить переменные в Autos

---

### Пример 3: Отладка физики

**Проблема:** Автомобиль не ускоряется

**Решение:**
1. Поставить точку останова в `CarPhysics.Accelerate()`
2. Запустить отладку
3. Проверить `currentSpeed`, `maxSpeed` в Watch
4. Пошагово выполнить расчёты (F10)
5. Проверить условия в if/else

---

## 📊 СРАВНЕНИЕ: ОТЛАДКА VS ЛОГИРОВАНИЕ

| Критерий | Отладка (Debugger) | Логирование (Logger) |
|----------|-------------------|---------------------|
| **Скорость** | Медленнее (требует паузы) | Быстрее (работает в реальном времени) |
| **Детализация** | Высокая (все переменные) | Средняя (только что логируется) |
| **Удобство** | Требует Visual Studio | Работает везде |
| **Применение** | Сложные баги | Отслеживание потока |
| **Производительность** | Влияет на FPS | Минимальное влияние |

**Рекомендация:** Использовать оба метода!

---

## 🚀 ПЛАН ВНЕДРЕНИЯ

### Приоритет 1: Базовая настройка (30 минут)

- [ ] Установить Visual Studio 2022 (если нет)
- [ ] Настроить подключение к Unity
- [ ] Создать `DEBUGGING_GUIDE.md`
- [ ] Добавить точки останова в ключевые методы

---

### Приоритет 2: Интеграция (1 час)

- [ ] Создать `debug-unity.ps1`
- [ ] Создать `debug-vs.ps1`
- [ ] Настроить `launch.json` для VS Code
- [ ] Добавить отладочные комментарии в код

---

### Приоритет 3: Документация (1 час)

- [ ] Создать `KNOWLEDGE_BASE/02_UNITY/UNITY_DEBUGGING_GUIDE.md`
- [ ] Создать `DEBUG_CHECKLIST.md`
- [ ] Добавить примеры отладки в отчёты

---

## 📚 СВЯЗАННЫЕ РЕСУРСЫ

### Из руководства Microsoft:
- [Отладка для абсолютных начинающих](https://docs.microsoft.com/visualstudio/debugger/getting-started-with-the-debugger)
- [Обзор отладчика Visual Studio](https://docs.microsoft.com/visualstudio/debugger/debugger-feature-tour)
- [Использование точек останова](https://docs.microsoft.com/visualstudio/debugger/using-breakpoints)

### Unity документация:
- [Отладка в Unity](https://docs.unity3d.com/Manual/ManagedCodeDebugging.html)
- [Профилирование](https://docs.unity3d.com/Manual/Profiler.html)

---

## ✅ ВЫВОДЫ

**Руководство Microsoft по отладке:**
- ✅ Подробное и пошаговое
- ✅ Применимо к C# и Unity
- ✅ Покрывает все основные функции

**Рекомендации для проекта:**
1. ✅ Создать `DEBUGGING_GUIDE.md`
2. ✅ Добавить отладочные скрипты
3. ✅ Настроить точки останова
4. ✅ Создать базу знаний по отладке
5. ✅ Интегрировать с существующими скриптами

**Время внедрения:** 2.5 часа

**Приоритет:** 🟡 Средний (после авторизации GitHub)

---

**Отчёт готов к внедрению!** 🐛

**Последнее обновление:** 28 февраля 2026 г.
