---
status: stable
created: 2026-02-28
last_reviewed: 2026-02-28
source: Methodology
---
# 🕵️ МЕТОДОЛОГИЯ АНАЛИЗА ЛОГОВ — C# / Unity

**Версия:** 1.0  
**Дата:** 28 февраля 2026 г.  
**Статус:** ✅ Обязательна к применению всеми агентами

---

## 🎯 ОБЩИЕ ПРИНЦИПЫ (Фундамент)

### 1. Иерархия поиска: От общего к частному

**НЕ читать логи подряд!** Действовать как следователь:

#### Шаг 1: Сканирование

**Искать маркеры:**
- ✅ `error`
- ✅ `exception`
- ✅ `fail`
- ✅ `exit code 1`
- ✅ `nullreference`
- ✅ `stacktrace`

**Команда:**
```bash
grep -i "error\|exception\|fail" logfile.txt
```

---

#### Шаг 2: Контекст

**Прочитать 10-20 строк ДО и ПОСЛЕ ошибки:**

```
[10 строк ДО] ← Причина часто здесь!
[ОШИБКА] ← Симптом
[10 строк ПОСЛЕ] ← Последствия
```

**Пример:**
```
[INFO] Загрузка сцены...
[INFO] Создание игрока...
[WARN] Компонент Rigidbody не найден  ← ПРИЧИНА!
[ERROR] NullReferenceException в PlayerController.Start()  ← СИМПТОМ!
[INFO] Попытка восстановления...
```

---

#### Шаг 3: Гипотеза и проверка

**Цикл рассуждений:**
```
1. Симптом: NullReferenceException в PlayerController.Start()
2. Гипотеза: Компонент не инициализирован
3. Проверка: Проверить код PlayerController.cs строка 45
4. Подтверждение: _rb = GetComponent<Rigidbody>() отсутствует
5. Решение: Добавить инициализацию в Start()
```

---

### 2. Итеративный подход: "Думай, прежде чем копать"

**Самая большая ошибка:** Сразу лезть в дебри!

**Правильный порядок:**

```
1. Сформулировать симптом (что говорит лог на поверхности)
2. Выдвинуть 1-2 гипотезы (вероятные причины)
3. Запросить доп. данные (код, сцена, другой лог)
4. Подтвердить/опровергнуть гипотезы
5. Выдать вердикт и план лечения
```

---

## 📜 СПЕЦИФИКА C# / UNITY

### 1. Типы логов Unity

| Лог | Когда читать | Что искать |
|-----|--------------|------------|
| **Editor.log** | Краш редактора, импорт ассетов | Ошибки импорта, краши Editor |
| **Player.log** | Ошибки в билде/игре | Debug.Log, исключения в runtime |
| **build.log** | Ошибки сборки | Ошибки компиляции, линковки |

---

### 2. Чтение стека вызовов (Stack Trace)

**Пример:**
```
NullReferenceException: Object reference not set to an instance of an object
  at PlayerController.Update () [0x00015] in Assets/Scripts/PlayerController.cs:45 
  at UnityEngine.ExecuteEvents.Execute (UnityEngine.GameObject target) [0x0006e]
  at UnityEngine.EventSystem.Update () [0x0008f]
```

**Как читать:**
```
Верхние строки (PlayerController.Update) ← Где "взорвалось"
Нижние строки (EventSystem.Update) ← Путь к ошибке
```

**Действия:**
1. Открыть `Assets/Scripts/PlayerController.cs`
2. Найти строку 45
3. Проверить что на этой строке
4. Найти причину null

---

### 3. Антипаттерны и частые ошибки

#### NullReferenceException

**Симптом:**
```
NullReferenceException: Object reference not set
  at PlayerController.Start()
```

**Причины:**
- ❌ Объект не инициализирован
- ❌ GetComponent не нашёл компонент
- ❌ Ссылка уничтожена (Destroy)

**Проверка:**
```csharp
// ❌ ПЛОХО
_rb.AddForce(10f);  // _rb может быть null!

// ✅ ХОРОШО
if (_rb != null)
{
    _rb.AddForce(10f);
}
else
{
    Debug.LogError("Rigidbody не найден!");
}
```

---

#### MissingReferenceException

**Симптом:**
```
MissingReferenceException: The object of type 'GameObject' has been destroyed
```

**Причина:**
- Объект уничтожен (Destroy), но ссылка осталась

**Решение:**
```csharp
// Проверка перед использованием
if (enemy != null && enemy.gameObject != null)
{
    enemy.TakeDamage(10);
}
```

---

#### IndexOutOfRangeException

**Симптом:**
```
IndexOutOfRangeException: Index was outside the bounds of the array
```

**Причина:**
- Выход за границы массива/списка в цикле

**Решение:**
```csharp
// ❌ ПЛОХО
for (int i = 0; i <= enemies.Length; i++)  // <= вместо <
{
    enemies[i].Attack();  // Ошибка на последнем элементе!
}

// ✅ ХОРОШО
for (int i = 0; i < enemies.Length; i++)
{
    enemies[i].Attack();
}
```

---

#### Ошибки сериализации

**Симптом:**
```
SerializationException: Type 'MyClass' is not marked as serializable
```

**Причина:**
- Класс не помечен [Serializable]

**Решение:**
```csharp
[Serializable]  // ← Добавить!
public class PlayerData
{
    public string name;
    public int level;
}
```

---

#### Ошибки IL2CPP

**Симптом:**
```
Error in IL2CPP code generation
```

**Причина:**
- Проблемы при компиляции в нативный код

**Решение:**
- Использовать Mono вместо IL2CPP для тестов
- Проверить код на совместимость с IL2CPP

---

## ✅ ПРАВИЛА ХОРОШЕГО ТОНА

### 1. Структура ответа

**Требовать формат:**

```markdown
### 📋 Резюме проблемы
[Одним предложением]

### 🔍 Анализ улик
- **Ошибка:** [Цитата ключевой строки]
- **Контекст:** [Что было до/после]
- **Вывод:** [Ваши выводы]

### 🎯 Наиболее вероятная причина
[Четкая формулировка]

### 📝 План действий
1. [Шаг 1: "Открой файл X"]
2. [Шаг 2: "Найди строку Y"]
3. [Шаг 3: "Проверь Z"]
4. [Шаг 4: "Исправь на W"]

### ✅ Проверка
[Как проверить что исправлено]
```

---

### 2. Честность и точность

**Запрещено гадать!**

**Если недостаточно данных:**
```
❌ ПЛОХО: "Возможно, проблема в этом, попробуйте то..."

✅ ХОРОШО: "Для точного диагноза мне нужно:
1. Код метода Update() в PlayerController.cs
2. Первые 50 строк лога (версия Unity, платформа)
3. Скриншот Inspector для объекта Player"
```

---

### 3. Горячие команды

**Для быстрой настройки:**

| Команда | Действие |
|---------|----------|
| `/сжать` | Сократить ответ |
| `/суть` | Только диагноз без лирики |
| `/чек` | Перепроверить ответ на логику |
| `/лог` | Проанализировать лог |
| `/ошибка` | Разобрать ошибку |

---

## 🚀 КАК ИЗБЕЖАТЬ ПЕРЕГРУЗКИ

### 1. Порционность

**НЕ скидывать мегабайтные логи целиком!**

**Просить ИИ:**
```
"Просканируй лог на критические ошибки
и выпиши только их с контекстом 10 строк"
```

**Инструменты:**
```bash
# Windows
findstr /C:"Error" /C:"Exception" logfile.txt

# Linux/Mac
grep -E "Error|Exception" logfile.txt

# PowerShell
Select-String -Pattern "Error|Exception" -Path logfile.txt -Context 10
```

---

### 2. Структурирование

**Использовать парсеры:**

```powershell
# Фильтр по типу
Get-Content logfile.txt | Where-Object { $_ -match "ERROR" }

# Контекст 10 строк
Select-String -Pattern "NullReference" -Path logfile.txt -Context 10
```

---

### 3. Фокус на паттернах

**Вместо объяснения каждой ошибки:**

**Дать общее правило:**
```
"NullReferenceException обычно означает:
1. Объект не создан (null)
2. Компонент не найден (GetComponent)
3. Ссылка уничтожена (Destroy)

Проверяй в этом порядке!"
```

---

### 4. Принцип "Достаточно хорошо"

**Не объяснять всё сразу!**

**Позволить учиться на ошибках:**
```
1. ИИ даёт решение
2. Пользователь проверяет
3. Если неверно → корректирует
4. ИИ запоминает опыт
5. В следующий раз лучше
```

---

## 🎯 ПРИМЕРЫ АНАЛИЗА

### Пример 1: NullReferenceException

**Лог:**
```
[INFO] Загрузка игры...
[INFO] Создание игрока...
[ERROR] NullReferenceException: Object reference not set
  at PlayerController.Start() [0x00015]
  at UnityEngine.GameObject.AddComponent()
```

**Анализ ИИ:**

```markdown
### 📋 Резюме проблемы
NullReferenceException при создании игрока в PlayerController.Start()

### 🔍 Анализ улик
- **Ошибка:** `NullReferenceException: Object reference not set`
- **Место:** `PlayerController.Start(), строка 15`
- **Контекст:** Ошибка при создании игрока

### 🎯 Наиболее вероятная причина
Компонент Rigidbody не найден через GetComponent в Start()

### 📝 План действий
1. Открой `Assets/Scripts/PlayerController.cs`
2. Найди строку 15 (метод Start)
3. Проверь: `_rb = GetComponent<Rigidbody>()`
4. Убедись что на объекте Player есть Rigidbody компонент
5. Если нет → добавь Rigidbody на объект Player

### ✅ Проверка
Запусти игру → ошибка должна исчезнуть
```

---

### Пример 2: Ошибка сборки

**Лог:**
```
[INFO] Building...
[ERROR] error CS0246: The type or namespace name 'Logger' could not be found
[INFO] Compilation failed
```

**Анализ ИИ:**

```markdown
### 📋 Резюме проблемы
Ошибка компиляции: не найден класс Logger

### 🔍 Анализ улик
- **Ошибка:** `error CS0246: The type or namespace name 'Logger' could not be found`
- **Тип:** Ошибка компиляции C#
- **Контекст:** Сборка проекта

### 🎯 Наиболее вероятная причина
1. Файл Logger.cs не в той папке
2. Отсутствует using директива
3. Класс Logger не public

### 📝 План действий
1. Проверь что `Logger.cs` лежит в `Assets/Scripts/Core/`
2. Проверь что класс public: `public class Logger`
3. Добавь using в файл где ошибка: `using YourNamespace;`

### ✅ Проверка
Запусти сборку → ошибка должна исчезнуть
```

---

## 📚 БАЗА ЗНАНИЙ АГЕНТОВ

### Обязательные файлы:

| Файл | Назначение |
|------|------------|
| **`LOG_ANALYSIS_METHODOLOGY.md`** | Этот файл (методология) |
| **`COMMON_UNITY_ERRORS.md`** | Частые ошибки Unity |
| **`CSTACK_TRACE_GUIDE.md`** | Чтение стек-трейсов C# |

---

## ✅ ЧЕКЛИСТ ДЛЯ АГЕНТОВ

### Перед анализом лога:

- [ ] Отфильтровать по error/exception
- [ ] Найти первую ошибку
- [ ] Прочитать контекст (10 строк до/после)
- [ ] Определить тип ошибки

### Перед ответом:

- [ ] Сформулировать симптом
- [ ] Выдвинуть гипотезу
- [ ] Проверить гипотезу
- [ ] Составить план действий
- [ ] Добавить проверку

### После ответа:

- [ ] Запомнить результат
- [ ] Обновить базу знаний
- [ ] Учиться на ошибке (если была)

---

**Методология принята!** 🕵️

**Все агенты обязаны применять!** 📋
