# 📖 БАЗА РЕШЁННЫХ ОШИБОК

**Версия:** 2.0 (с диагностикой)  
**Дата:** 28 февраля 2026 г.

---

## 🎯 НАЗНАЧЕНИЕ

Этот файл — **живая база знаний** по ошибкам и их решениям.

**Правило:** Каждая решённая ошибка добавляется сюда!

---

## 📋 ФОРМАТ ЗАПИСИ

```markdown
## [Категория]

### Ошибка: [Симптом]
**Причина:** [Диагноз]

**Диагностика:**
```powershell
.\unity-query-scene.ps1 -target "Canvas/MainMenu/PlayButton"
```
Ожидаемый ответ: `children: 0` (нет дочерних объектов)

**Решение:**
```powershell
.\unity-execute-code.ps1 -code "AddTextToButton.cs"
```

**Файл:** [Путь к файлу]
**Дата:** [Дата]
```

---

## 🔧 ИНСТРУМЕНТЫ ДИАНОСТИКИ

| Инструмент | Команда | Описание |
|------------|---------|----------|
| **Query Scene** | `.\unity-query-scene.ps1` | Проверка объектов на сцене |
| **Get Logs** | `.\unity-get-logs.ps1` | Получение логов консоли |
| **Execute Code** | `.\unity-execute-code.ps1` | Выполнение кода в Unity |

---

## 📝 БЫСТРЫЕ КОМАНДЫ

| Команда | Действие |
|---------|----------|
| `/need_data` | Запросить уточнение у пользователя |
| `/update` | Предложить добавить новое знание в базу |
| `/debug [ошибка]` | Режим анализа ошибки с использованием этой базы |

---

## UI/Кнопки

### Ошибка: Кнопка не подсвечивается при наведении

**Причина:** Не настроен `Navigation` в компоненте Button

**Диагностика:**
```powershell
.\unity-query-scene.ps1 -target "Canvas/MainMenu/NewGameButton"
```
Ожидаемый ответ: Проверить наличие компонента Button и настройки Navigation

**Решение:**
```csharp
var navigation = button.navigation;
navigation.mode = Navigation.Mode.Automatic;
button.navigation = navigation;
```

**Файл:** `Assets/Scripts/UI/MainMenuController.cs`
**Дата:** 28.02.2026

---

### Ошибка: Текст на кнопке не отображается

**Причина:** Не установлен TextMeshPro Essentials или не выбран шрифт

**Диагностика:**
```powershell
.\unity-query-scene.ps1 -target "Canvas/MainMenu/PlayButton"
.\unity-get-logs.ps1 -type error
```
Ожидаемый ответ: `textMeshPro: false` или ошибки в логах

**Решение:**
1. `Window` → `TextMeshPro` → `Import TMP Essentials`
2. В инспекторе кнопки выбрать шрифт в поле "Font Asset"

**Файл:** `Assets/Scenes/MainMenu.unity`
**Дата:** 28.02.2026

---

### Ошибка: Кнопка не реагирует на клик мыши

**Причина:** Нет ссылки в `On Click ()` в инспекторе кнопки

**Диагностика:**
```powershell
.\unity-query-scene.ps1 -target "Canvas/MainMenu/PlayButton"
```
Ожидаемый ответ: Проверить наличие Button.onClick

**Решение:**
1. Выбрать кнопку в иерархии
2. В инспекторе, компонент `Button`
3. Развернуть `On Click ()`
4. Нажать `+`
5. Перетащить объект с обработчиком (например, `MainMenuManager`)
6. Выбрать функцию: `MainMenuController.OnNewGame()`

**Файл:** `Assets/Scenes/MainMenu.unity`
**Дата:** 28.02.2026

---

### Ошибка: Клавиатура не работает (↑↓Enter не реагируют)

**Причина:** Не настроена навигация между кнопками

**Диагностика:**
```powershell
.\unity-query-scene.ps1 -target "Canvas/MainMenu/NewGameButton"
```
Ожидаемый ответ: Проверить Navigation.Select On Up/Down

**Решение:**
Для каждой кнопки настроить `Navigation`:
1. Выбрать кнопку
2. В инспекторе, компонент `Button`
3. Развернуть `Navigation`
4. Режим: `Automatic`
5. Заполнить `Select On Up`, `Select On Down`

**Файл:** `Assets/Scenes/MainMenu.unity`
**Дата:** 28.02.2026

---

### Ошибка: Кнопка не отображается на сцене (нет дочернего TextMeshPro)

**Причина:** Кнопка создана без текстового компонента

**Диагностика:**
```powershell
.\unity-query-scene.ps1 -target "Canvas/MainMenu/PlayButton"
```
Ожидаемый ответ: `children: 0` (нет дочерних объектов)

**Решение:**

**Вариант A: Через редактор**
1. Выбрать кнопку в иерархии
2. `GameObject` → `UI` → `Text - TextMeshPro`
3. Перетащить текст на кнопку
4. Настроить RectTransform

**Вариант B: Через код**
```powershell
.\unity-execute-code.ps1 -code @"
using UnityEngine;
using TMPro;

public class AddTextToButton : MonoBehaviour
{
    void Start()
    {
        Button btn = GetComponent<Button>();
        if (btn != null)
        {
            GameObject textGO = new GameObject("ButtonText", typeof(RectTransform), typeof(TextMeshProUGUI));
            textGO.transform.SetParent(transform, false);
            
            TextMeshProUGUI tmp = textGO.GetComponent<TextMeshProUGUI>();
            tmp.text = "Play";
            tmp.fontSize = 24;
            tmp.color = Color.white;
            tmp.alignment = TextAlignmentOptions.Center;
            
            RectTransform rect = textGO.GetComponent<RectTransform>();
            rect.anchorMin = Vector2.zero;
            rect.anchorMax = Vector2.one;
            rect.sizeDelta = Vector2.zero;
        }
    }
}
"@
```

**Файл:** `Assets/Scenes/MainMenu.unity`
**Дата:** 28.02.2026

---

## Сборка

### Ошибка: BuildReport не найден
**Причина:** Отсутствует `using UnityEditor.Build.Reporting;`
**Решение:** Добавить using в начало файла:
```csharp
using UnityEditor.Build.Reporting;
```
**Файл:** `Assets/Scripts/Editor/BuildScript.cs`
**Дата:** 27.02.2026

---

### Ошибка: BuildResult не найден
**Причина:** Используется устаревшее имя типа
**Решение:** Заменить `BuildResult` на `BuildResultCode`
```csharp
// Было:
if (report.summary.result == BuildResult.Succeeded)

// Стало:
if (report.summary.result == BuildResultCode.Succeeded)
```
**Файл:** `Assets/Scripts/Editor/BuildScript.cs`
**Дата:** 27.02.2026

---

### Ошибка: Ошибка компиляции после сборки
**Причина:** В логах есть ошибки, но сборка продолжается
**Решение:** Использовать автосборку с исправлением ошибок:
```powershell
.\auto-build-exe.ps1
```
Скрипт автоматически:
1. Очищает логи
2. Запускает сборку
3. Анализирует ошибки
4. Исправляет (если возможно)
5. Повторяет до успеха

**Файл:** `auto-build-exe.ps1`
**Дата:** 28.02.2026

---

## Анализаторы кода

### Ошибка: SA1101 Используйте this.
**Причина:** StyleCop требует явное указание `this.` для полей класса
**Решение:**
Вариант A: Добавить `this.`:
```csharp
// Было:
myField = 5;

// Стало:
this.myField = 5;
```

Вариант B: Отключить правило в `.editorconfig`:
```ini
dotnet_diagnostic.SA1101.severity = none
```

**Файл:** `.editorconfig`
**Дата:** 27.02.2026

---

### Ошибка: IDE0090 Используйте var
**Причина:** Анализатор предлагает использовать неявную типизацию
**Решение:**
```csharp
// Было:
List<string> myList = new List<string>();

// Стало:
var myList = new List<string>();
```

**Файл:** `Assets/Scripts/*.cs`
**Дата:** 27.02.2026

---

### Ошибка: S1135 Завершите задачу (TODO)
**Причина:** В коде есть комментарии `// TODO`
**Решение:**
1. Выполнить задачу из TODO
2. Удалить комментарий
3. Или заменить на `// FIXME` если задача критична

**Файл:** `Assets/Scripts/*.cs`
**Дата:** 27.02.2026

---

## Сохранения

### Ошибка: SaveSystem не сохраняет данные
**Причина:** Не указан путь для сохранений или нет папки
**Решение:**
```csharp
// Проверка пути:
string path = Path.Combine(Application.persistentDataPath, "saves");
if (!Directory.Exists(path))
{
    Directory.CreateDirectory(path);
}
```

**Файл:** `Assets/Scripts/SaveSystem/SaveSystem.cs`
**Дата:** 28.02.2026

---

### Ошибка: JSON не сериализуется
**Причина:** Класс не имеет атрибута `[Serializable]`
**Решение:**
```csharp
[Serializable]
public class PlayerData
{
    public string playerName;
    public int level;
}
```

**Файл:** `Assets/Scripts/Data/PlayerData.cs`
**Дата:** 28.02.2026

---

## 📊 СТАТИСТИКА

| Категория | Ошибок | Решено |
|-----------|--------|--------|
| UI/Кнопки | 4 | 4 |
| Сборка | 3 | 3 |
| Анализаторы | 3 | 3 |
| Сохранения | 2 | 2 |
| **Итого** | **12** | **12** |

---

## 🔧 КАК ДОБАВИТЬ НОВУЮ ЗАПИСЬ

1. Найди категорию (или создай новую)
2. Скопируй формат записи выше
3. Заполни поля:
   - **Ошибка:** Симптом (что видит пользователь)
   - **Причина:** Диагноз (почему это произошло)
   - **Решение:** Код или действие
   - **Файл:** Где была ошибка
   - **Дата:** Когда исправлено

4. Сохрани файл

---

**Правило:** Каждая исправленная ошибка → добавляется сюда! ✅

**Последнее обновление:** 28 февраля 2026 г.
