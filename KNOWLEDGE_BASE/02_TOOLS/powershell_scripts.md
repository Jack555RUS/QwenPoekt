---
status: stable
created: 2026-02-28
last_reviewed: 2026-02-28
source: Tools Documentation
---
# 🛠️ POWERSHELL SCRIPTS — ИНСТРУМЕНТЫ

**Версия:** 1.0  
**Дата:** 28 февраля 2026 г.

---

## 🎯 НАЗНАЧЕНИЕ

Этот файл описывает **скрипты автоматизации** проекта.

---

## 📋 СПИСОК СКРИПТОВ

| Скрипт | Назначение | Путь |
|--------|------------|------|
| [`auto-build-exe.ps1`](#auto-build-exeps1) | Сборка EXE | `PROJECTS/DragRaceUnity/` |
| [`auto-build.ps1`](#auto-buildps1) | Автосборка | `PROJECTS/DragRaceUnity/` |
| [`clean-logs.ps1`](#clean-logsp1) | Очистка логов | `D:\QwenPoekt\` |
| [`check-environment.ps1`](#check-environmentps1) | Проверка окружения | `D:\QwenPoekt\` |
| [`setup-spell-checker.ps1`](#setup-spell-checkerps1) | Настройка орфографии | `D:\QwenPoekt\` |
| [`extract-knowledge-from-old.ps1`](#extract-knowledge-from-oldps1) | Извлечение знаний | `D:\QwenPoekt\` |

---

## 📖 ОПИСАНИЕ СКРИПТОВ

### auto-build-exe.ps1

**Назначение:** Автоматическая сборка EXE файла.

**Путь:** `PROJECTS/DragRaceUnity/auto-build-exe.ps1`

**Что делает:**
1. Очищает логи перед сборкой
2. Запускает Unity в режиме батча
3. Создаёт билд в `Build/DragRace.exe`
4. Логирует процесс

**Использование:**
```powershell
cd PROJECTS\DragRaceUnity
.\auto-build-exe.ps1
```

**Параметры:**
- Нет (использует настройки по умолчанию)

**Выходной файл:** `Build/DragRace.exe`

---

### auto-build.ps1

**Назначение:** Автосборка с проверкой компиляции.

**Путь:** `PROJECTS/DragRaceUnity/auto-build.ps1`

**Что делает:**
1. Запускает компиляцию в Unity
2. Проверяет логи на ошибки
3. При ошибках → анализирует и исправляет
4. Повторяет до успеха

**Использование:**
```powershell
cd PROJECTS\DragRaceUnity
.\auto-build.ps1
```

**Отличие от auto-build-exe.ps1:**
- `auto-build.ps1` → только компиляция (быстро)
- `auto-build-exe.ps1` → компиляция + создание EXE (долго)

---

### clean-logs.ps1

**Назначение:** Очистка логов и временных файлов.

**Путь:** `D:\QwenPoekt\clean-logs.ps1`

**Что делает:**
1. Удаляет `*.log` из корня
2. Удаляет логи из проекта
3. Очищает `Library/`, `Temp/`, `obj/`, `Logs/`
4. Очищает `.vs/`

**Использование:**
```powershell
.\clean-logs.ps1
```

**Когда использовать:**
- ⏰ Перед каждой сборкой
- ⏰ При проблемах с компиляцией

---

### check-environment.ps1

**Назначение:** Проверка окружения.

**Путь:** `D:\QwenPoekt\check-environment.ps1`

**Что проверяет:**
- ✅ Unity установлен
- ✅ Visual Studio установлен
- ✅ .NET SDK установлен
- ✅ Расширения VS Code (Code Spell Checker)
- ✅ Анализаторы (StyleCop, SonarLint)
- ✅ Incredibuild
- ✅ Структура проекта
- ✅ Документация

**Использование:**
```powershell
.\check-environment.ps1
```

**Когда использовать:**
- ⏰ При первом запуске
- ⏰ После обновления инструментов
- ⏰ При проблемах со сборкой

---

### setup-spell-checker.ps1

**Назначение:** Настройка Code Spell Checker.

**Путь:** `D:\QwenPoekt\setup-spell-checker.ps1`

**Что делает:**
1. Создаёт папку `%APPDATA%\Code\User\`
2. Настраивает `settings.json` для VS Code
3. Включает проверку русского языка
4. Добавляет технические слова в словарь

**Использование:**
```powershell
.\setup-spell-checker.ps1
```

**Когда использовать:**
- ⏰ При первой установке VS Code
- ⏰ При проблемах с проверкой орфографии

---

### extract-knowledge-from-old.ps1

**Назначение:** Извлечение знаний из папки OLD/.

**Путь:** `D:\QwenPoekt\extract-knowledge-from-old.ps1`

**Что делает:**
1. Создаёт папки для документации Unity
2. Копирует документацию Input System, UGUI, TextMeshPro
3. Копирует инструкции для ИИ
4. Копирует ценные отчёты
5. Очищает мусор (Library, obj, bin, Build, Temp)

**Использование:**
```powershell
.\extract-knowledge-from-old.ps1
```

**Когда использовать:**
- ⏰ Один раз при настройке проекта

---

## 🔄 РАБОЧИЙ ПРОЦЕСС

### Стандартная сборка:

```powershell
# 1. Очистить логи
.\clean-logs.ps1

# 2. Проверить окружение (если нужно)
.\check-environment.ps1

# 3. Собрать проект (компиляция)
cd PROJECTS\DragRaceUnity
.\auto-build.ps1

# 4. Собрать EXE (финальный билд)
.\auto-build-exe.ps1

# 5. Запустить билд
.\Build\DragRace.exe
```

---

## 📊 СРАВНЕНИЕ СКРИПТОВ

| Скрипт | Время | Что делает | Когда |
|--------|-------|------------|-------|
| **clean-logs.ps1** | 5 сек | Очистка мусора | Перед сборкой |
| **check-environment.ps1** | 10 сек | Проверка всего | При проблемах |
| **auto-build.ps1** | 1-2 мин | Компиляция | Часто |
| **auto-build-exe.ps1** | 5-10 мин | EXE билд | Перед тестом |
| **setup-spell-checker.ps1** | 5 сек | Настройка | Один раз |
| **extract-knowledge-from-old.ps1** | 1 мин | Извлечение знаний | Один раз |

---

## 🐛 ТИПИЧНЫЕ ОШИБКИ

### Ошибка: "Скрипт не может быть выполнен"

**Причина:** PowerShell блокирует выполнение скриптов.

**Решение:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

### Ошибка: "Unity не найден"

**Причина:** Неверный путь к Unity в скрипте.

**Решение:** Открой `auto-build-exe.ps1`, найди строку:
```powershell
$unityPath = "C:\Program Files\Unity\Hub\Editor\6000.3.10f1\Editor\Unity.exe"
```
Исправь путь на свой.

---

### Ошибка: "Access denied"

**Причина:** Файлы заблокированы Unity.

**Решение:**
1. Закрой Unity
2. Запусти скрипт снова

---

## 🔗 СВЯЗАННЫЕ ФАЙЛЫ

- [`00_CORE/csharp_standards.md`](./00_CORE/csharp_standards.md) — Стандарты кода
- [`01_RULES/logging_standards.md`](./01_RULES/logging_standards.md) — Логирование
- [`03_PATTERNS/error_solutions.md`](./03_PATTERNS/error_solutions.md) — База ошибок

---

**Правило:** Используй скрипты для автоматизации! ✅

**Последнее обновление:** 28 февраля 2026 г.
