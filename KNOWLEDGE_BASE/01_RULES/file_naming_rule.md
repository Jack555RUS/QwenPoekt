---
status: stable
created: 2026-03-01
last_reviewed: 2026-03-01
topic: File and Folder Naming Convention
---

# 📁 ПРАВИЛО: ИМЕНОВАНИЕ ФАЙЛОВ И ПАПОК

**Тип:** Обязательное правило  
**Применение:** Все файлы и папки проекта

---

## 🎯 НАЗНАЧЕНИЕ

**Цель:** Единый стандарт именования для избежания ошибок совместимости.

**Проблемы, которые решает:**
- ❌ Ошибки Windows MAX_PATH (260 символов)
- ❌ Проблемы с кодировкой (кириллица в путях)
- ❌ Сложности с инструментами (пробелы в именах)
- ❌ Путаница в стилях (PascalCase vs snake_case)

---

## 📋 ОСНОВНЫЕ ПРАВИЛА

### 1. Стиль именования

| Объект | Стиль | Пример |
|--------|-------|--------|
| **Файлы .md, .cs, .json** | snake_case | `file_naming_rule.md` |
| **Файлы .ps1** | snake_case | `check_duplicates.ps1` |
| **Файлы .unity, .uxml, .uss** | PascalCase | `MainMenu.unity` |
| **Папки** | PascalCase | `KNOWLEDGE_BASE/`, `Scripts/` |
| **Классы C#** | PascalCase | `MainMenuController` |
| **Методы C#** | PascalCase | `OnNewGameClicked()` |

**Почему snake_case для файлов:**
- ✅ Читаемость: `file_naming_rule.md` лучше `filenamingrule.md`
- ✅ Визуальное разделение: `_` чётче разделяет слова, чем `-`
- ✅ Совместимость: работает везде (Windows, Linux, Git)
- ✅ Нет конфликтов: не путается с PascalCase (классы)

---

### 2. Ограничение длины пути

**Правило:** Полный путь ≤ **150 символов** (не 260!).

**Почему 150, а не 260:**
```
MAX_PATH (Windows) = 260 символов

Запас нужен для:
- Временные папки при сборке: +50
- Глубокая вложенность при компиляции: +40
- Резервные копии с датой: +20
───────────────────────────────
Итого запас: ~110 символов
Оставляем: 150 символов
```

**Примеры:**

```
✅ ПРАВИЛЬНО (85 символов):
D:\QwenPoekt\KNOWLEDGE_BASE\01_RULES\file_naming_rule.md

❌ НЕПРАВИЛЬНО (180 символов):
D:\QwenPoekt\KNOWLEDGE_BASE\01_RULES\very_long_file_name_that_will_cause_problems_with_windows_path_limit_when_building_or_backing_up.md
```

**Проверка перед созданием:**
```powershell
# Проверить длину пути
$filePath = "D:\QwenPoekt\KNOWLEDGE_BASE\01_RULES\file_naming_rule.md"
Write-Host "Длина пути: $($filePath.Length) символов"
if ($filePath.Length -gt 150) {
    Write-Warning "Путь слишком длинный! Сократите."
}
```

---

### 3. Только латиница

**Правило:** Имена файлов и папок — **только ASCII** (a-z, A-Z, 0-9, -, _).

**Почему:**
- ✅ Совместимость с Git (кириллица ломает diff)
- ✅ Работает на всех ОС (Windows, Linux, macOS)
- ✅ Инструменты не выдают ошибки кодировки

**Исключения:**
- ⚠️ **Содержимое файлов** — любой язык (UTF-8)
- ⚠️ **Комментарии в коде** — любой язык

**Примеры:**

```
✅ ПРАВИЛЬНО:
file-naming-rule.md
ИИ_ИНСТРУКЦИЯ_ДЛЯ_БУДУЩИХ_ПРОЕКТОВ.md  ← историческое, не менять!

❌ НЕПРАВИЛЬНО (новые файлы):
правило-именования-файлов.md
файл-для-теста.md
```

---

### 4. Никаких пробелов

**Правило:** Пробелы заменяются на `_` (underscore).

**Почему:**
- ✅ Пробелы ломают CLI команды
- ✅ Требуют экранирования в скриптах
- ✅ Ошибки в URL (веб-документация)
- ✅ `_` согласуется с snake_case (единый стиль)

**Примеры:**

```
✅ ПРАВИЛЬНО:
file_naming_rule.md
check_duplicates.ps1

❌ НЕПРАВИЛЬНО:
file naming rule.md
check duplicates.ps1
```

---

### 5. Префиксы для типов файлов

**Рекомендация:** Добавлять префиксы для группировки:

| Префикс | Назначение | Пример |
|---------|------------|--------|
| `rule_` | Правила | `rule-file-naming.md` |
| `pattern_` | Паттерны | `pattern-error-handling.md` |
| `guide_` | Руководства | `guide-debugging.md` |
| `template_` | Шаблоны | `template-integration-report.md` |

**Но лучше:** Использовать структуру папок:
```
KNOWLEDGE_BASE/
├── 01_RULES/
│   └── file-naming-rule.md      ← ясно из папки
├── 03_PATTERNS/
│   └── error-handling.md
```

---

### 6. Запрещённые символы (PowerShell/cmd/Unix)

**Правило:** Избегать символов со специальным значением в оболочках и инструментах.

**Почему:** Эти символы требуют экранирования, что усложняет скрипты и повышает риск ошибок.

#### Таблица запрещённых символов:

| Символ | Название | Проблема в PowerShell | Проблема в cmd | Проблема в Unix |
|--------|----------|----------------------|----------------|-----------------|
| ` ` (пробел) | Пробел | Разделяет аргументы | Разделяет аргументы | Разделяет аргументы |
| `&` | Амперсанд | Фоновое выполнение | Разделитель команд | Фоновое выполнение |
| `\|` | Вертикальная черта | Конвейер (pipe) | Конвейер (pipe) | Конвейер (pipe) |
| `>` | Больше | Перенаправление вывода | Перенаправление вывода | Перенаправление вывода |
| `<` | Меньше | Перенаправление ввода | Перенаправление ввода | Перенаправление ввода |
| `*` | Звёздочка | Wildcard (все файлы) | Wildcard | Wildcard |
| `?` | Вопрос | Wildcard (1 символ) | Wildcard | Wildcard |
| `[` `]` | Скобки квадратные | Шаблоны, массивы | Шаблоны | Шаблоны regex |
| `{` `}` | Скобки фигурные | Блоки кода | Литеры | Шаблоны |
| `(` `)` | Скобки круглые | Группировка | Литеры | Подсеть |
| `$` | Доллар | Переменные (`$var`) | Литера | Переменные |
| `@` | Собака | Splatting, массивы | Литера | Литера |
| `#` | Решётка | Комментарии | Литера | Комментарии |
| `;` | Точка с запятой | Разделитель команд | Разделитель команд | Разделитель команд |
| `=` | Равно | Присваивание | Литера | Присваивание |
| `!` | Восклицание | История, NOT | Литера | История bash |
| `%` | Процент | Остаток, циклы | Переменные (`%VAR%`) | Переменные |
| `^` | Карет | XOR, экранирование | Экранирование | Экранирование |
| `` ` `` | Обратный апостроф | Экранирование | Литера | Команды (backtick) |
| `"` `'` | Кавычки | Строки | Строки | Строки |
| `:` | Двоеточие | Разделитель (C:), stream | Литера | Путь (URL) |
| `,` | Запятая | Разделитель массивов | Литера | Разделитель |
| `/` | Слэш прямой | Ключи (`-f`) | Разделитель пути | Разделитель пути |
| `\` | Слэш обратный | Разделитель пути | Разделитель пути | Экранирование |

**Важно:** Даже если символ работает в Windows, он может ломать:
- ✅ Git на Linux
- ✅ CI/CD пайплайны
- ✅ Docker контейнеры
- ✅ Веб-серверы
- ✅ AI инструменты

#### Разрешённые символы:

```
✅ Только:
- Латинские буквы: a-z, A-Z
- Цифры: 0-9
- Подчёркивание: _
- Дефис: - (не в начале)
- Точка: . (только для расширения)
```

---

### 7. Регистр букв

**Правило:** Только **строчные** (lowercase) буквы для имён файлов.

**Почему:**

| Причина | Объяснение |
|---------|------------|
| **Windows** | Регистр не важен, но для совместимости |
| **Linux** | Регистр важен: `File.md` ≠ `file.md` |
| **Git** | Предупреждает о смене регистра (`file.md` → `File.md`) |
| **Веб-серверы** | Часто регистрозависимы (Apache, Nginx) |
| **URL** | `%D0%A4` vs `%D1%84` — разные символы |
| **Компиляторы** | Некоторые чувствительны к регистру |

**Примеры:**

```
✅ ПРАВИЛЬНО:
file_naming_rule.md
check_duplicates.ps1
main_menu_controller.cs
config.local.json

❌ НЕПРАВИЛЬНО:
File_Naming_Rule.md      ← PascalCase для файлов
file-naming-RULE.md      ← смешанный регистр
MainMenuController.cs    ← PascalCase (должно быть main_menu_controller.cs)
```

**Исключения:**

| Объект | Регистр | Пример |
|--------|---------|--------|
| **Файлы** | lowercase | `file_naming_rule.md` |
| **Папки** | PascalCase | `KNOWLEDGE_BASE/` |
| **Классы C#** | PascalCase | `MainMenuController` |
| **Методы C#** | PascalCase | `OnNewGameClicked()` |

---

### 8. Экранирование проблемных имён

**Если файл уже с проблемным именем** (получен извне, устаревший):

#### PowerShell:

```powershell
# Вариант 1: Одинарные кавычки (предпочтительно)
Get-ChildItem 'C:\My Files\data[2025].csv'
Copy-Item 'file with spaces.txt' 'backup.txt'

# Вариант 2: Двойные кавычки (для переменных)
$filename = "data"
Get-ChildItem "C:\My Files\$filename.csv"

# Вариант 3: Экранирование обратным апострофом
Get-ChildItem C:\My` Files\data`[2025`].csv

# Вариант 4: Оператор --% (отключить интерпретацию)
some.exe --% "C:\My Files\data[2025].csv"
```

#### cmd (командная строка):

```cmd
REM Вариант 1: Двойные кавычки
dir "C:\My Files\data[2025].csv"
copy "file with spaces.txt" "backup.txt"

REM Вариант 2: Экранирование ^
dir C:\My^ Files\data^[2025^].csv
```

#### Unix/Linux (bash):

```bash
# Вариант 1: Кавычки
ls "/home/user/my files/data[2025].csv"

# Вариант 2: Экранирование обратным слэшем
ls /home/user/my\ files/data\[2025\].csv
```

**⚠️ НО ЛУЧШЕ:** Переименовать файл!

```powershell
# Переименовать в безопасное имя
Rename-Item 'file with spaces.txt' 'file_with_spaces.txt'
Rename-Item 'data[2025].csv' 'data_2025.csv'
```

---

### 9. Скрипт проверки безопасности имён

**Автоматическая проверка файлов на соответствие правилу:**

```powershell
# check-safe-filename.ps1
param(
    [string]$Path = ".",
    [switch]$Recurse
)

$forbiddenChars = '[&|><*$@#;=!%`"''(),\[\]{}:]'
$cyrillic = '[А-Яа-яЁё]'
$spaces = '\s'

$files = if ($Recurse) {
    Get-ChildItem -Path $Path -Recurse -File
} else {
    Get-ChildItem -Path $Path -File
}

$hasErrors = $false

foreach ($file in $files) {
    $name = $file.Name
    
    # Проверка на запрещённые символы
    if ($name -match $forbiddenChars) {
        Write-Host "❌ $($file.FullName) — запрещённые символы" -ForegroundColor Red
        $hasErrors = $true
    }
    
    # Проверка на кириллицу
    if ($name -match $cyrillic) {
        Write-Host "⚠️  $($file.FullName) — кириллица (только латиница)" -ForegroundColor Yellow
    }
    
    # Проверка на пробелы
    if ($name -match $spaces) {
        Write-Host "⚠️  $($file.FullName) — пробелы (замените на _)" -ForegroundColor Yellow
    }
    
    # Проверка на заглавные буквы (для файлов)
    if ($file.Extension -match '\.(md|cs|ps1|json|xml|yaml|yml)$' -and $name -match '[A-Z]') {
        Write-Host "⚠️  $($file.FullName) — заглавные буквы (только lowercase)" -ForegroundColor Yellow
    }
}

if (!$hasErrors) {
    Write-Host "✅ Все имена файлов безопасны" -ForegroundColor Green
} else {
    Write-Host "`n❌ Найдены проблемы! Переименуйте файлы." -ForegroundColor Red
    exit 1
}
```

**Использование:**

```powershell
# Проверить текущую папку
.\check-safe-filename.ps1

# Проверить папку рекурсивно
.\check-safe-filename.ps1 -Path "D:\QwenPoekt\KNOWLEDGE_BASE" -Recurse

# Проверить перед коммитом
.\check-safe-filename.ps1 -Recurse
```

---

## 🔄 ПЕРЕИМЕНОВАНИЕ (ЕСЛИ НУЖНО)

### ⚠️ ПРЕДУПРЕЖДЕНИЕ

**Переименование ломает:**
- Ссылки в других файлах
- Git историю (файл считается новым)
- Скрипты, которые ссылаются на файл

### Чек-лист перед переименованием:

```
[ ] 1. ПРОВЕРКА НА СУЩЕСТВОВАНИЕ ССЫЛОК
    [ ] grep_search по всем .md файлам
    [ ] grep_search по .ps1 скриптам
    [ ] grep_search по .cs файлам

[ ] 2. АНАЛИЗ ЗАВИСИМОСТЕЙ
    [ ] Какие файлы ссылаются на этот?
    [ ] Есть ли скрипты, которые используют этот путь?
    [ ] Упомянут ли файл в документации?

[ ] 3. ОБНОВЛЕНИЕ ССЫЛОК
    [ ] Обновить все найденные ссылки
    [ ] Проверить относительные пути

[ ] 4. ТЕСТИРОВАНИЕ
    [ ] Запустить скрипты, которые ссылаются на файл
    [ ] Открыть файлы, проверить ссылки
    [ ] Сделать git status, проверить изменения

[ ] 5. КОММИТ
    [ ] Закоммитить переименование + обновление ссылок
    [ ] В сообщении указать: "Rename X → Y (update Z links)"
```

---

## 🛠 ИНСТРУМЕНТЫ ПРОВЕРКИ

### Скрипт проверки длины пути:

```powershell
# check-path-length.ps1
param(
    [string]$Path = ".",
    [int]$MaxLength = 150
)

Get-ChildItem -Recurse -File | ForEach-Object {
    $length = $_.FullName.Length
    if ($length -gt $MaxLength) {
        Write-Warning "$($_.FullName) — $length символов (превышает $MaxLength)"
    }
}
```

---

### Скрипт поиска ссылок на файл:

```powershell
# find-file-references.ps1
param(
    [string]$FileName
)

Write-Host "Поиск ссылок на '$FileName'..."

# Поиск в .md файлах
grep_search -pattern $FileName -glob "*.md"

# Поиск в .ps1 скриптах
grep_search -pattern $FileName -glob "*.ps1"

# Поиск в .cs файлах
grep_search -pattern $FileName -glob "*.cs"
```

---

## 📊 ПРИМЕРЫ ПРИМЕНЕНИЯ

### Пример 1: Создание нового файла

**Задача:** Создать правило по именованию.

**Применение правила:**

```
1. ВЫБОР ИМЕНИ:
   ❌ FileNamingRule.md (PascalCase)
   ❌ file-naming-rule.md (kebab-case)
   ✅ file_naming_rule.md (snake_case)

2. ПРОВЕРКА ДЛИНЫ ПУТИ:
   Путь: D:\QwenPoekt\KNOWLEDGE_BASE\01_RULES\file_naming_rule.md
   Длина: 68 символов ✅ (меньше 150)

3. ПРОВЕРКА НА ЛАТИНИЦУ:
   ✅ Только ASCII символы

4. ПРОВЕРКА ПРОБЕЛОВ:
   ✅ Нет пробелов

5. ИТОГ: Создаём file_naming_rule.md
```

---

### Пример 2: Переименование файла

**Задача:** Переименовать `ui_toolkit_rules.md` → `ui_toolkit_rules_v2.md`.

**Применение правила:**

```
1. ПРОВЕРКА ССЫЛОК:
   grep_search("ui_toolkit_rules.md") → найдено 5 ссылок:
   - AI_START_HERE.md
   - csharp_standards.md
   - before_change_rule.md
   
2. АНАЛИЗ ЗАВИСИМОСТЕЙ:
   - 3 файла ссылаются
   - Скрипты не ссылаются
   - Git история: не важно (можно переименовать)

3. ПЛАН ДЕЙСТВИЙ:
   a) Обновить 3 ссылки в файлах
   b) Переименовать файл
   c) Закоммитить всё вместе

4. ВЫПОЛНЕНИЕ:
   - Обновить ссылки (edit в каждом файле)
   - Переименовать файл
   - git add . && git commit -m "Rename ui_toolkit_rules → ui_toolkit_rules_v2"
```

---

### Пример 3: Создание папки

**Задача:** Создать папку для новых правил.

**Применение правила:**

```
1. ВЫБОР ИМЕНИ:
   ❌ новые_правила/ (кириллица)
   ❌ new rules/ (пробел)
   ✅ NewRules/ (PascalCase — рекомендуется для папок)
   ✅ new_rules/ (snake_case — допустимо)

2. ПРОВЕРКА ДЛИНЫ:
   Путь: D:\QwenPoekt\KNOWLEDGE_BASE\01_RULES\NewRules\
   Длина: 52 символа ✅

3. ИТОГ: Создаём NewRules/
```

---

## 🚫 ТИПИЧНЫЕ ОШИБКИ

### Ошибка 1: Смешивание стилей

```
❌ ПЛОХО:
KNOWLEDGE_BASE/01_RULES/file_naming_rule.md      ← snake_case
KNOWLEDGE_BASE/01_RULES/UI-TOOLKIT-RULES.md      ← UPPER-kebab-case
KNOWLEDGE_BASE/01_RULES/BeforeChangeRule.md      ← PascalCase

✅ ХОРОШО (единый snake_case):
KNOWLEDGE_BASE/01_RULES/file_naming_rule.md      ← snake_case
KNOWLEDGE_BASE/01_RULES/ui_toolkit_rules.md      ← snake_case
KNOWLEDGE_BASE/01_RULES/before_change_rule.md    ← snake_case
```

---

### Ошибка 2: Слишком длинные пути

```
❌ ПЛОХО:
D:\QwenPoekt\KNOWLEDGE_BASE\02_UNITY\INPUT_SYSTEM\ADVANCED_TOPICS\TROUBLESHOOTING\COMMON_MISTAKES_AND_SOLUTIONS.md
(147 символов — уже близко к пределу!)

✅ ХОРОШО:
D:\QwenPoekt\KNOWLEDGE_BASE\02_UNITY\INPUT_SYSTEM\TROUBLESHOOTING\common-mistakes.md
(89 символов — есть запас)
```

---

### Ошибка 3: Кириллица в именах

```
❌ ПЛОХО:
KNOWLEDGE_BASE/правила/именование.md
scripts/проверка-дубликатов.ps1

✅ ХОРОШО:
KNOWLEDGE_BASE/rules/naming.md
scripts/check-duplicates.ps1

⚠️ ИСКЛЮЧЕНИЕ (исторические):
ИИ_ИНСТРУКЦИЯ_ДЛЯ_БУДУЩИХ_ПРОЕКТОВ.md  ← уже есть, не менять!
```

---

## 🔗 СВЯЗАННЫЕ ПРАВИЛА

- [`before_change_rule.md`](./before_change_rule.md) — Проверка перед внесением изменений
- [`META_RULES_FOR_KNOWLEDGE_ANALYSIS.md`](../05_METHODOLOGY/META_RULES_FOR_KNOWLEDGE_ANALYSIS.md) — Упоминание kebab-case
- [`csharp_standards.md`](../00_CORE/csharp_standards.md) — Именование в коде C#

---

## 📝 ИСТОРИЯ ИЗМЕНЕНИЙ

| Дата | Изменение |
|------|-----------|
| 2026-03-01 19:00 | Создано правило |
| 2026-03-01 20:45 | Дополнено: раздел 6 (запрещённые символы, 20+ символов) |
| 2026-03-01 20:45 | Дополнено: раздел 7 (регистр букв, только lowercase) |
| 2026-03-01 20:45 | Дополнено: раздел 8 (экранирование в PowerShell/cmd/Unix) |
| 2026-03-01 20:45 | Дополнено: раздел 9 (скрипт проверки безопасности) |

---

**Правило обязательно к применению для всех новых файлов!** ✅

**Для существующих файлов:** Не переименовывать без необходимости (см. чек-лист выше).

**Последнее обновление:** 1 марта 2026 г. (версия 1.1)
