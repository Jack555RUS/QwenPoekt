---
status: stable
created: 2026-03-01
last_reviewed: 2026-03-01
topic: Before Change Verification Rule
---

# ✅ ПРАВИЛО: ПРОВЕРКА ПЕРЕД ВНЕСЕНИЕМ ИЗМЕНЕНИЙ

**Тип:** Обязательное правило
**Применение:** Код, база знаний, настройки, конфигурации

---

## 🎯 НАЗНАЧЕНИЕ

**Цель:** Избежать дублирования, конфликтов и непреднамеренных поломок.

**Принцип:** *Семь раз отмерь — один раз отрежь.*

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ШАГИ ПЕРЕД ИЗМЕНЕНИЯМИ

### Шаг 1. Проверка на существование

**Вопрос:** Возможно, это уже есть?

**Действия:**

| Что вносим | Как проверить | Инструменты |
|------------|---------------|-------------|
| **Код (метод, класс)** | Поиск по кодовой базе | `grep_search`, `glob` |
| **Правило в базу знаний** | Поиск по `KNOWLEDGE_BASE/` | `grep_search`, `check-duplicates.ps1` |
| **Настройка в конфиг** | Чтение конфига | `read_file` |
| **Переменная, константа** | Поиск по проекту | `grep_search` |

**Пример:**
```powershell
# Перед созданием метода CheckForDuplicates
# Проверим, нет ли уже такого:
grep_search(pattern: "CheckForDuplicates|CheckDuplicate|IsDuplicate")
```

**Если найдено:**
- ✅ Использовать существующее
- ✅ Дополнить, если функционала недостаточно
- ❌ Не создавать дубликат

---

### Шаг 2. Анализ зависимостей

**Вопрос:** Что ещё использует этот компонент?

**Действия:**

1. **Найти все ссылки:**
   ```powershell
   grep_search(pattern: "MethodName|ClassName")
   ```

2. **Проверить цепочки вызовов:**
   - Кто вызывает этот метод?
   - Какие методы вызывает он?
   - Есть ли побочные эффекты?

3. **Оценить влияние:**
   | Изменение | Возможные последствия |
   |-----------|----------------------|
   | Удаление метода | Сломаются все вызовы |
   | Изменение сигнатуры | Потребуется обновить вызовы |
   | Изменение логики | Могут измениться результаты |

---

### Шаг 3. Логический анализ результатов

**Вопрос:** Что изменится до и после?

**Чек-лист:**

```
[ ] Понимаю ли я текущее состояние системы?
[ ] Вижу ли я все зависимости?
[ ] Предвижу ли я побочные эффекты?
[ ] Есть ли тесты, которые это покрывают?
[ ] Что сломается, если я это изменю?
[ ] Можно ли достичь цели без изменений?
```

**Пример анализа:**

> **Задача:** Добавить логирование в метод `Save()`
>
> **Анализ:**
> - Метод вызывается в 5 местах
> - Изменение сигнатуры потребует правки всех вызовов
> - Логирование может замедлить работу (I/O операция)
> - Возможно, лучше добавить отдельный метод `SaveWithLogging()`
>
> **Решение:** Добавить логирование внутри существующего метода, но сделать его опциональным через параметр.

---

### Шаг 4. Проверка на необходимость

**Вопрос:** Действительно ли это нужно?

**Критерии необходимости:**

| Ситуация | Необходимо? |
|----------|-------------|
| Исправление ошибки | ✅ Да |
| Новая фича по ТЗ | ✅ Да |
| Улучшение без запроса | ⚠️ Обсудить |
| Дублирование функционала | ❌ Нет |
| Изменение работающего кода без причины | ❌ Нет |

**Правило:** Если можно не менять — не меняй.

---

## 🚫 ТИПИЧНЫЕ ОШИБКИ

### Ошибка 1: Создание дубликатов

```csharp
// ❌ ПЛОХО: Уже есть метод CheckForDuplicates()
public void VerifyNoDuplicates() { }  // Дубликат!

// ✅ ХОРОШО: Используем существующий
CheckForDuplicates();
```

---

### Ошибка 2: Изменение без анализа зависимостей

```csharp
// Было:
public void Save(string path) { }

// ❌ ПЛОХО: Изменили сигнатуру без проверки вызовов
public void Save(string path, bool overwrite) { }
// Теперь все 5 вызовов Save(path) сломаются!

// ✅ ХОРОШО: Добавили перегрузку
public void Save(string path, bool overwrite = true) { }
```

---

### Ошибка 3: Нарушение цепочки событий

```csharp
// Было:
void OnClick() {
    SaveData();      // 1. Сохранение
    UpdateUI();      // 2. Обновление UI
    LogAction();     // 3. Логирование
}

// ❌ ПЛОХО: Убрали SaveData(), думая что не нужно
void OnClick() {
    UpdateUI();      // Теперь UI обновляется с старыми данными!
    LogAction();
}

// ✅ ХОРОШО: Понимаем цепочку, сохраняем порядок
void OnClick() {
    SaveData();      // Сначала сохраняем
    UpdateUI();      // Потом обновляем
    LogAction();     // Логируем
}
```

---

## 🛠 ИНСТРУМЕНТЫ ПРОВЕРКИ

### Для кода:

| Инструмент | Команда | Назначение |
|------------|---------|------------|
| **Grep Search** | `grep_search(pattern: "...")` | Поиск по коду |
| **Glob** | `glob(pattern: "**/*.cs")` | Поиск файлов |
| **Read File** | `read_file(path: "...")` | Чтение файла |
| **Git Diff** | `git diff HEAD` | Просмотр изменений |

---

### Для базы знаний:

| Инструмент | Команда | Назначение |
|------------|---------|------------|
| **Grep Search** | `grep_search(path: "KNOWLEDGE_BASE")` | Поиск дубликатов |
| **Check Duplicates** | `.\scripts\check-duplicates.ps1` | Автоматическая проверка |
| **List Directory** | `list_directory(path: "...")` | Просмотр структуры |

---

## 📊 ЧЕК-ЛИСТ ПЕРЕД ВНЕСЕНИЕМ

**Обязательно выполнить перед любым изменением:**

```
[ ] 1. ПРОВЕРКА НА СУЩЕСТВОВАНИЕ
    [ ] Поиском в базе/коде
    [ ] С помощью скриптов
    [ ] Визуальный просмотр

[ ] 2. АНАЛИЗ ЗАВИСИМОСТЕЙ
    [ ] Кто использует этот компонент?
    [ ] Какие есть побочные эффекты?
    [ ] Что сломается при изменении?

[ ] 3. ЛОГИЧЕСКИЙ АНАЛИЗ
    [ ] Что будет ДО изменений?
    [ ] Что будет ПОСЛЕ изменений?
    [ ] Все ли последствия учтены?

[ ] 4. ПРОВЕРКА НЕОБХОДИМОСТИ
    [ ] Действительно ли это нужно?
    [ ] Можно ли достичь цели без изменений?
    [ ] Не нарушит ли это работу других компонентов?
```

---

## 🔗 СВЯЗАННЫЕ ПРАВИЛА

- [`META_RULES_FOR_KNOWLEDGE_ANALYSIS.md`](../05_METHODOLOGY/META_RULES_FOR_KNOWLEDGE_ANALYSIS.md) — Проверка дубликатов в базе знаний
- [`AI_SELF_LEARNING_METHODOLOGY.md`](../05_METHODOLOGY/AI_SELF_LEARNING_METHODOLOGY.md) — Интеграция без дублирования (DRY)
- [`csharp_standards.md`](../00_CORE/csharp_standards.md) — Стандарты кода C#
- [`error_solutions.md`](../03_PATTERNS/error_solutions.md) — База решений ошибок

---

## 🎯 ПРИМЕРЫ ПРИМЕНЕНИЯ

### Пример 1: Добавление метода

**Задача:** Добавить метод проверки дубликатов.

**Применение правила:**

```
1. ПРОВЕРКА НА СУЩЕСТВОВАНИЕ:
   → grep_search("CheckDuplicate") → найдено 3 метода
   → Вывод: Уже есть функционал!

2. АНАЛИЗ ЗАВИСИМОСТЕЙ:
   → Существующие методы используются в 10 местах
   → Вывод: Нельзя удалять или менять сигнатуру

3. ЛОГИЧЕСКИЙ АНАЛИЗ:
   → ДО: Есть 3 метода для разных типов данных
   → ПОСЛЕ: Нужен универсальный метод?
   → Вывод: Лучше расширить существующие

4. ПРОВЕРКА НЕОБХОДИМОСТИ:
   → Можно ли использовать существующие? ✅ Да
   → Решение: Дополнить существующие методы
```

---

### Пример 2: Изменение настройки

**Задача:** Изменить параметр `MaxRetries` в конфиге.

**Применение правила:**

```
1. ПРОВЕРКА НА СУЩЕСТВОВАНИЕ:
   → read_file("config.json") → параметр есть, значение = 3

2. АНАЛИЗ ЗАВИСИМОСТЕЙ:
   → grep_search("MaxRetries") → используется в 5 файлах
   → Вывод: Изменение повлияет на 5 компонентов

3. ЛОГИЧЕСКИЙ АНАЛИЗ:
   → ДО: 3 попытки, таймаут 30 сек
   → ПОСЛЕ: 10 попыток, таймаут 100 сек
   → Последствия: Увеличение времени выполнения при ошибке

4. ПРОВЕРКА НЕОБХОДИМОСТИ:
   → Зачем менять? → Частые таймауты
   → Можно ли без изменений? → Оптимизировать причину таймаутов
   → Решение: Увеличить до 5, но сначала исправить причину
```

---

**Правило обязательно к применению!** ✅

**Последнее обновление:** 1 марта 2026 г.
