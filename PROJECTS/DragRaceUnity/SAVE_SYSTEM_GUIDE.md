# 💾 Система сохранений — Документация

**Версия:** 1.0
**Дата:** 28 февраля 2026 г.
**Статус:** ✅ ГОТОВО

---

## 📖 Описание

Система сохранений поддерживает 5 слотов и использует JSON для сериализации данных.

---

## 🎯 Возможности

- ✅ 5 слотов сохранений
- ✅ JSON сериализация
- ✅ Автоматическая дата сохранения
- ✅ Проверка целостности данных
- ✅ Статистика игрока (победы, поражения, время)
- ✅ Система уровней и опыта
- ✅ Деньги и покупки
- ✅ Разблокировка автомобилей

---

## 📁 Файлы

| Файл | Назначение |
|------|------------|
| **PlayerData.cs** | Модель данных игрока |
| **SaveSystem.cs** | Система сохранений |
| **SaveSystemTests.cs** | Тесты системы |

---

## 💿 Как использовать

### Сохранение:

```csharp
// Создаём данные
PlayerData data = new PlayerData
{
    PlayerName = "Hero",
    Level = 5,
    Money = 2500
};

// Сохраняем в слот 0
bool saved = SaveSystem.Save(0, data);

if (saved)
{
    Logger.Info("Сохранение успешно!", null);
}
```

### Загрузка:

```csharp
// Проверяем наличие сохранения
if (SaveSystem.HasSave(0))
{
    // Загружаем из слота 0
    PlayerData data = SaveSystem.Load(0);
    
    Logger.Info($"Игрок: {data.PlayerName}", null);
    Logger.Info($"Уровень: {data.Level}", null);
    Logger.Info($"Деньги: {data.Money}", null);
}
```

### Удаление:

```csharp
// Удаляем сохранение из слота 0
SaveSystem.Delete(0);
```

### Получение информации:

```csharp
// Информация о конкретном слоте
string info = SaveSystem.GetSaveInfo(0);
// "Слот 0: Hero (Ур. 5) - 2026-02-28 12:34:56"

// Информация о всех слотах
string[] allSaves = SaveSystem.GetAllSaveInfo();

foreach (string save in allSaves)
{
    Logger.Info(save, null);
}
```

---

## 🎮 PlayerData — Данные игрока

### Основные поля:

| Поле | Тип | Описание |
|------|-----|----------|
| **PlayerId** | string | Уникальный ID |
| **PlayerName** | string | Имя игрока |
| **Level** | int | Уровень |
| **Experience** | int | Опыт |
| **Money** | int | Деньги |
| **CurrentCarId** | int | Текущий автомобиль |
| **UnlockedCars** | int[] | Разблокированные авто |
| **LastSaveDate** | string | Дата сохранения |
| **TotalPlayTime** | float | Время игры (сек) |
| **RacesWon** | int | Побед в гонках |
| **RacesLost** | int | Поражений в гонках |

### Методы:

```csharp
// Добавить опыт
data.AddExperience(500);

// Добавить деньги
data.AddMoney(1000);

// Потратить деньги
bool spent = data.SpendMoney(500);

// Разблокировать автомобиль
data.UnlockCar(5);

// Проверить автомобиль
bool unlocked = data.IsCarUnlocked(5);

// Записать победу
data.RecordWin();

// Записать поражение
data.RecordLoss();
```

---

## 📍 Расположение сохранений

Сохранения хранятся в папке:

```
D:\QwenPoekt\PROJECTS\DragRaceUnity\Build\DragRace_Data\SaveGames\
```

Файлы:
- `SaveSlot_0.save` — Слот 0
- `SaveSlot_1.save` — Слот 1
- `SaveSlot_2.save` — Слот 2
- `SaveSlot_3.save` — Слот 3
- `SaveSlot_4.save` — Слот 4

---

## 🧪 Тесты

### Запуск тестов:

```
1. Unity → Window → General → Test Runner
2. Вкладка: Edit Mode
3. Выберите: SaveSystemTests
4. Нажмите: Run Selected
```

### Покрытие тестами:

- ✅ Сохранение валидных данных
- ✅ Загрузка существующего сохранения
- ✅ Удаление сохранения
- ✅ Проверка наличия сохранения
- ✅ Сохранение в неверный слот
- ✅ Загрузка из пустого слота
- ✅ PlayerData: опыт и уровни
- ✅ PlayerData: деньги
- ✅ PlayerData: автомобили
- ✅ PlayerData: статистика гонок
- ✅ Круговое сохранение/загрузка

---

## 🔒 Безопасность

**Важно:** Текущая реализация использует простой JSON. Для продакшена рекомендуется:

1. **Шифрование:**
```csharp
string encrypted = Encrypt(json);
File.WriteAllText(path, encrypted);
```

2. **Контрольные суммы:**
```csharp
string checksum = CalculateMD5(json);
```

3. **Резервное копирование:**
```csharp
File.Copy(path, path + ".bak");
```

---

## 🐛 Обработка ошибок

Система автоматически обрабатывает:

- ❌ Неверный номер слота
- ❌ Отсутствие файла сохранения
- ❌ Повреждённые данные
- ❌ Ошибки диска

Все ошибки логируются через Logger.

---

## 📊 Статистика

Система автоматически отслеживает:

- Время игры
- Победы/поражения
- Дата последнего сохранения
- Прогресс игрока (уровень, опыт)

---

## 🎯 Интеграция с меню

Кнопки меню используют SaveSystem:

| Кнопка | Действие |
|--------|----------|
| **Новая игра** | Создаёт PlayerData, сохраняет в слот 0 |
| **Продолжить** | Загружает из слота 0 |
| **Сохранить** | Сохраняет текущие данные |
| **Загрузить** | Показывает информацию о слотах |

---

**Система сохранений готова к использованию!** 💾
