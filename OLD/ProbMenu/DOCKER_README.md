# Docker Documentation для ProbMenu

## 📋 Обзор

Этот проект использует Docker для:
- **Контейнеризации WinForms приложения** (меню)
- **Сборки Unity проекта** (DragRaceUnity)
- **Базы данных** (PostgreSQL) для сохранений и статистики
- **Кэша** (Redis) для временных данных
- **CI/CD** автоматизации через GitHub Actions

---

## 🚀 Быстрый старт

### Предварительные требования

- **Docker Desktop** (Windows/Mac) или **Docker Engine** (Linux)
- **Docker Compose** (входит в Docker Desktop)
- **PowerShell** (Windows) или **Bash** (Linux/Mac)

### 1. Клонирование и настройка

```bash
# Копирование переменных окружения
cp .env.example .env

# Отредактируйте .env и установите ваши значения
# Особенно UNITY_EMAIL, UNITY_PASSWORD если нужна сборка Unity
```

### 2. Запуск приложения

```bash
# Быстрый запуск (только приложение)
docker compose --profile app up -d

# Режим разработчика (с БД и Adminer)
docker compose --profile dev up -d

# Полный режим (все сервисы)
docker compose --profile full up -d
```

### 3. Проверка статуса

```bash
# Статус контейнеров
docker compose ps

# Просмотр логов
docker compose logs -f
```

---

## 📁 Профили Docker Compose

| Профиль | Сервисы | Описание |
|---------|---------|----------|
| `app` | probmenu, redis, postgres | Только приложение с зависимостями |
| `dev` | app + adminer | Разработка с доступом к БД |
| `full` | все сервисы | Полная среда |
| `test` | test-runner | Запуск тестов |
| `unity` | unity-builder | Сборка Unity проекта |
| `build` | unity-builder | Альтернатива для сборки |

---

## 🛠️ Скрипты автоматизации

### Windows (PowerShell)

```powershell
# Сборка Docker образов
.\docker\scripts\build.ps1

# Запуск (профили: app, dev, full, test, unity)
.\docker\scripts\run.ps1 -Profile dev -Detach

# Остановка
.\docker\scripts\stop.ps1 -RemoveVolumes

# Тестирование
.\docker\scripts\test.ps1 -Verbose

# Сборка Unity
.\docker\scripts\unity-build.ps1 -BuildName DragRace -Clean
```

### Windows (.bat)

```batch
REM Сборка
docker\scripts\build.bat

REM Запуск
docker\scripts\run.bat dev

REM Остановка
docker\scripts\stop.bat

REM Тесты
docker\scripts\test.bat

REM Unity сборка
docker\scripts\unity-build.bat DragRace Win64
```

### Linux/WSL (Bash)

```bash
# Сборка
./docker/scripts/build.sh

# Запуск
./docker/scripts/run.sh dev

# Остановка
./docker/scripts/stop.sh

# Тесты
./docker/scripts/test.sh

# Unity сборка
./docker/scripts/unity-build.sh DragRace Win64
```

---

## 🗄️ База данных

### Подключение к PostgreSQL

```bash
# Из хоста
psql -h localhost -U postgres -d probmenu

# Из контейнера
docker compose exec postgres psql -U postgres -d probmenu
```

### Adminer (веб-интерфейс)

Доступен при запуске с профилем `dev` или `full`:
- **URL:** http://localhost:8080
- **Система:** PostgreSQL
- **Сервер:** postgres
- **Пользователь:** postgres
- **Пароль:** postgres123
- **База:** probmenu

### Redis

```bash
# Подключение к Redis CLI
docker compose exec redis redis-cli

# Из хоста
redis-cli -h localhost -p 6379
```

---

## 🏗️ Сборка Unity проекта

### Требования

- **Unity License** (личная или профессиональная)
- **Лицензионный файл** (опционально для CI/CD)

### Локальная сборка

```bash
# Через скрипт
.\docker\scripts\unity-build.ps1 -BuildName DragRace -Platform Win64

# Или напрямую через docker compose
docker compose --profile unity up --build
```

### Получение Unity License

1. Перейдите на https://license.unity3d.com/manual
2. Войдите в аккаунт Unity
3. Скачайте файл лицензии (`.ulf`)
4. Конвертируйте в base64 (для GitHub Actions):
   ```bash
   base64 -i Unity_liceness.ulf
   ```
5. Добавьте в GitHub Secrets как `UNITY_LICENSE`

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Через скрипт
.\docker\scripts\test.ps1

# Или напрямую
docker compose --profile test run --rm test-runner
```

### Результаты тестов

Результаты сохраняются в папку `TestResults/`

---

## 🔧 Полезные команды Docker

```bash
# Просмотр логов
docker compose logs -f probmenu
docker compose logs -f postgres

# Остановка всех контейнеров
docker compose down

# Полная очистка (с удалением томов)
docker compose down -v

# Пересборка образа
docker compose build --no-cache

# Вход в контейнер
docker compose exec probmenu bash
docker compose exec postgres bash

# Статистика использования ресурсов
docker stats

# Очистка неиспользуемых ресурсов
docker system prune -a
```

---

## 📊 Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     Docker Network                          │
│                    probmenu-network                         │
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │  probmenu   │───▶│   redis     │    │  postgres   │     │
│  │  (WinForms) │    │  (Cache)    │    │     (DB)    │     │
│  │  :80        │    │   :6379     │    │    :5432    │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│         ▲                                      ▲            │
│         │                                      │            │
│         └──────────────────┬───────────────────┘            │
│                            │                                │
│                    ┌───────────────┐                        │
│                    │    adminer    │                        │
│                    │    :8080      │                        │
│                    └───────────────┘                        │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              unity-builder (отдельный)              │   │
│  │              DragRaceUnity → Builds/                │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 CI/CD Pipeline

GitHub Actions автоматически:

1. **Lint** — валидация Docker-файлов
2. **Build & Test** — сборка и тестирование .NET
3. **Docker Build** — сборка и публикация образа
4. **Unity Build** — сборка игры (для тегов)
5. **Deploy** — деплой на production

### Триггеры

- Push в `main`/`master`/`develop`
- Pull Request
- Теги версий (`v*.*.*`)
- Ручной запуск через UI

---

## 🐛 Отладка

### Частые проблемы

#### 1. Docker не запускается

```bash
# Проверка статуса Docker
docker info

# Перезапуск Docker Desktop
# Windows: клик правой кнопкой на иконке → Restart
```

#### 2. Ошибка порта

```bash
# Проверка занятых портов
netstat -ano | findstr :8080
netstat -ano | findstr :5432

# Измените порты в docker-compose.yml
```

#### 3. Unity сборка не работает

```bash
# Проверка лицензии
echo $UNITY_LICENSE

# Проверка логов
docker compose logs unity-builder
```

#### 4. База данных не подключается

```bash
# Проверка здоровья БД
docker compose exec postgres pg_isready

# Перезапуск PostgreSQL
docker compose restart postgres
```

---

## 📝 Переменные окружения

Создайте файл `.env` на основе `.env.example`:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `UNITY_EMAIL` | Email Unity аккаунта | - |
| `UNITY_PASSWORD` | Пароль Unity аккаунта | - |
| `UNITY_LICENSE` | Лицензионный файл (base64) | - |
| `POSTGRES_DB` | Имя базы данных | probmenu |
| `POSTGRES_USER` | Пользователь БД | postgres |
| `POSTGRES_PASSWORD` | Пароль БД | postgres123 |
| `DOTNET_ENVIRONMENT` | Среда .NET | Development |

---

## 📄 Структура Docker-файлов

```
ProbMenu/
├── Dockerfile                 # Основной образ WinForms
├── Dockerfile.Unity           # Образ для Unity сборки
├── docker-compose.yml         # Оркестрация сервисов
├── .dockerignore              # Исключения для Docker
├── .env.example               # Шаблон переменных окружения
├── docker/
│   ├── postgres/
│   │   └── init.sql          # Скрипт инициализации БД
│   └── scripts/
│       ├── build.ps1/sh/sh.bat
│       ├── run.ps1/sh/sh.bat
│       ├── stop.ps1/sh/sh.bat
│       ├── test.ps1/sh/sh.bat
│       └── unity-build.ps1/sh/sh.bat
└── .github/
    └── workflows/
        └── ci-cd.yml         # GitHub Actions pipeline
```

---

## 🎯 Следующие шаги

1. ✅ Настроить `.env` с вашими данными
2. ✅ Запустить `docker compose --profile dev up -d`
3. ✅ Проверить Adminer: http://localhost:8080
4. ✅ Протестировать приложение
5. ✅ Настроить CI/CD в GitHub

---

## 📞 Поддержка

- **Issues:** https://github.com/your-repo/ProbMenu/issues
- **Документация:** https://github.com/your-repo/ProbMenu/wiki
