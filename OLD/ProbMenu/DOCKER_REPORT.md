# 🐳 Docker Infrastructure - Отчёт о развёртывании

**Дата:** 2026-02-27  
**Статус:** ✅ ЗАВЕРШЕНО  
**Версия Docker:** 29.2.1

---

## ✅ Выполненные задачи

### 1. Docker-файлы

| Файл | Описание | Статус |
|------|----------|--------|
| `Dockerfile` | Многоэтапная сборка WinForms .NET 10 | ✅ Создан |
| `Dockerfile.Unity` | Образ для сборки Unity проекта | ✅ Создан |
| `docker-compose.yml` | Оркестрация всех сервисов | ✅ Создан |
| `.dockerignore` | Исключения для Docker-контекста | ✅ Создан |
| `docker/postgres/init.sql` | Скрипт инициализации БД | ✅ Создан |

### 2. Скрипты автоматизации

#### Windows (PowerShell)
- ✅ `docker/scripts/build.ps1` — Сборка Docker-образов
- ✅ `docker/scripts/run.ps1` — Запуск контейнеров
- ✅ `docker/scripts/stop.ps1` — Остановка и очистка
- ✅ `docker/scripts/test.ps1` — Запуск тестов
- ✅ `docker/scripts/unity-build.ps1` — Сборка Unity

#### Windows (.bat)
- ✅ `docker/scripts/build.bat`
- ✅ `docker/scripts/run.bat`
- ✅ `docker/scripts/stop.bat`
- ✅ `docker/scripts/test.bat`
- ✅ `docker/scripts/unity-build.bat`

#### Linux/WSL (Bash)
- ✅ `docker/scripts/build.sh`
- ✅ `docker/scripts/run.sh`
- ✅ `docker/scripts/stop.sh`
- ✅ `docker/scripts/test.sh`
- ✅ `docker/scripts/unity-build.sh`

### 3. CI/CD Pipeline

| Файл | Описание | Статус |
|------|----------|--------|
| `.github/workflows/ci-cd.yml` | GitHub Actions workflow | ✅ Создан |
| `.github/ISSUE_TEMPLATE/docker-issue.md` | Шаблон issue | ✅ Создан |

### 4. Конфигурация

| Файл | Описание | Статус |
|------|----------|--------|
| `.env.example` | Шаблон переменных окружения | ✅ Создан |
| `DOCKER_README.md` | Полная документация | ✅ Создан |

### 5. Тестирование

```
✅ Docker установлен: Docker version 29.2.1
✅ Docker-демон запущен
✅ Сборка образа probmenu:test успешна (300MB / 83.2MB сжатый)
✅ docker-compose.yml валидирован
✅ .dockerignore настроен (исключает DragRaceUnity/)
```

---

## 📊 Архитектура Docker

```
┌─────────────────────────────────────────────────────────┐
│                   Docker Network                        │
│                  probmenu-network                       │
│                                                         │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐ │
│  │  probmenu   │───▶│   redis     │    │  postgres   │ │
│  │  (WinForms) │    │  (Cache)    │    │     (DB)    │ │
│  │  :80        │    │   :6379     │    │    :5432    │ │
│  └─────────────┘    └─────────────┘    └─────────────┘ │
│         ▲                                      ▲        │
│         │                                      │        │
│         └──────────────────┬───────────────────┘        │
│                            │                            │
│                    ┌───────────────┐                    │
│                    │    adminer    │                    │
│                    │    :8080      │                    │
│                    └───────────────┘                    │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │            unity-builder (отдельно)               │ │
│  │            DragRaceUnity → Builds/                │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 Быстрый старт

### 1. Настройка окружения

```bash
# Копирование шаблона переменных окружения
cp .env.example .env

# Отредактируйте .env при необходимости
```

### 2. Запуск приложения

```bash
# Режим разработки (с БД и Adminer)
docker compose --profile dev up -d

# Проверка статуса
docker compose ps

# Просмотр логов
docker compose logs -f
```

### 3. Использование скриптов (Windows)

```powershell
# Сборка
.\docker\scripts\build.ps1

# Запуск (режим разработчика)
.\docker\scripts\run.ps1 -Profile dev -Detach

# Остановка
.\docker\scripts\stop.ps1

# Тесты
.\docker\scripts\test.ps1

# Сборка Unity
.\docker\scripts\unity-build.ps1 -BuildName DragRace
```

---

## 📁 Профили Docker Compose

| Профиль | Сервисы | Описание |
|---------|---------|----------|
| `app` | probmenu, redis, postgres | Только приложение |
| `dev` | app + adminer | Разработка с доступом к БД |
| `full` | все сервисы | Полная среда |
| `test` | test-runner | Запуск тестов |
| `unity` | unity-builder | Сборка Unity проекта |

---

## 🗄️ База данных

### PostgreSQL

- **Хост:** localhost
- **Порт:** 5432
- **База:** probmenu
- **Пользователь:** postgres
- **Пароль:** postgres123

### Adminer (веб-интерфейс)

- **URL:** http://localhost:8080
- **Система:** PostgreSQL
- **Сервер:** postgres

### Redis

- **Хост:** localhost
- **Порт:** 6379

---

## 📦 Docker-образы

### probmenu (основной)

- **Базовый образ:** mcr.microsoft.com/dotnet/runtime:10.0
- **Размер:** 300MB (83.2MB сжатый)
- **Платформа:** Linux (для тестов/CI), Windows (для запуска WinForms)

### unity-builder

- **Базовый образ:** gameci/unity-builder:2022.3-win
- **Назначение:** Сборка Unity проекта
- **Требует:** UNITY_LICENSE

---

## 🔧 CI/CD Pipeline

GitHub Actions автоматически выполняет:

1. **Lint** — валидация Docker-файлов
2. **Build & Test** — сборка и тестирование .NET
3. **Docker Build** — сборка и публикация образа
4. **Unity Build** — сборка игры (для тегов)
5. **Deploy** — деплой на production

### Триггеры

- Push в `main`/`master`/`develop`
- Pull Request
- Теги версий (`v*.*.*`)
- Ручной запуск

---

## 📝 Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
# Unity License (требуется для сборки Unity)
UNITY_EMAIL=your-email@example.com
UNITY_PASSWORD=your-password
UNITY_LICENSE=

# PostgreSQL
POSTGRES_DB=probmenu
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123

# Приложение
DOTNET_ENVIRONMENT=Development
```

---

## 🛠️ Полезные команды

```bash
# Статус контейнеров
docker compose ps

# Логи
docker compose logs -f

# Остановка
docker compose down

# Полная очистка (с томами)
docker compose down -v

# Вход в контейнер
docker compose exec postgres bash

# Статистика
docker stats

# Очистка системы
docker system prune -a
```

---

## 📄 Структура файлов

```
ProbMenu/
├── Dockerfile                      # Основной образ WinForms
├── Dockerfile.Unity                # Образ для Unity сборки
├── docker-compose.yml              # Оркестрация сервисов
├── .dockerignore                   # Исключения для Docker
├── .env.example                    # Шаблон переменных
├── DOCKER_README.md                # Документация
├── DOCKER_REPORT.md                # Этот отчёт
├── docker/
│   ├── postgres/
│   │   └── init.sql               # Скрипт инициализации БД
│   └── scripts/
│       ├── build.ps1/sh/bat       # Сборка
│       ├── run.ps1/sh/bat         # Запуск
│       ├── stop.ps1/sh/bat        # Остановка
│       ├── test.ps1/sh/bat        # Тесты
│       └── unity-build.ps1/sh/bat # Unity сборка
└── .github/
    └── workflows/
        └── ci-cd.yml              # GitHub Actions pipeline
```

---

## ✅ Проверка работоспособности

```bash
# 1. Проверка Docker
docker --version
# Output: Docker version 29.2.1

# 2. Проверка docker-compose
docker compose config
# Output: name: probmenu (валидная конфигурация)

# 3. Сборка образа
docker build -f Dockerfile -t probmenu:test .
# Output: Successfully built!

# 4. Проверка образа
docker images probmenu
# Output: probmenu:test  300MB
```

---

## 🎯 Следующие шаги

1. ✅ Настроить `.env` с вашими данными
2. ✅ Запустить `docker compose --profile dev up -d`
3. ✅ Проверить Adminer: http://localhost:8080
4. ✅ Протестировать приложение
5. ✅ Настроить CI/CD в GitHub

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker compose logs`
2. Проверьте статус: `docker compose ps`
3. Перезапустите: `docker compose restart`

**Документация:** `DOCKER_README.md`

---

**Статус:** ✅ Docker инфраструктура полностью готова к использованию!
