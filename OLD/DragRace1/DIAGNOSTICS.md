# 🔧 ДИАГНОСТИКА ПРОБЛЕМ

**Проблема 1:** Игра запускается во весь экран  
**Проблема 2:** Кнопка не нажимается

---

## ✅ ЧТО ИСПРАВЛЕНО:

### 1. GameSettings перемещён в Start()
```
Было: ApplySettings() в Awake()
Стало: ApplySettings() в Start()
Причина: Screen ещё не готов в Awake()
```

### 2. Проверка сцены Start.unity
```
✅ StartSceneController на Canvas
✅ UIDiagnostics отключён
✅ CanvasScaler на 1920×1080
```

---

## 📝 ПЛАНЫ ПО ДИАГНОСТИКЕ:

### После пересборки:

**1. Запустите игру:**
```
D:\QwenPoekt\DragRace\Builds\DragRace_v0.5\DragRace.exe
```

**2. Проверьте окно:**
```
[ ] Окно 1280×720 (не на весь экран)
[ ] Можно видеть границы окна
[ ] Можно свернуть (Alt+Tab)
```

**3. Проверьте кнопку:**
```
[ ] Зелёная кнопка START видна
[ ] Курсор меняется на "руку" при наведении
[ ] Клик мышью работает
[ ] Enter/Space работает
```

**4. Проверьте логи:**
```
Player.log: %USERPROFILE%\AppData\LocalLow\DefaultCompany\DragRace\Player.log

Ищите:
✅ Оконный режим: 1280x720
✅ Кнопка настроена: StartButton
✅ Слушателей: 1
```

---

## 🐛 ВОЗМОЖНЫЕ ПРИЧИНЫ:

### Окно на весь экран:

**Причина 1:** GameSettings не выполняется
```
Решение: Проверить логи на "GAME SETTINGS INITIALIZED"
```

**Причина 2:** Конфликт с CanvasScaler
```
Решение: CanvasScaler использует 1920×1080, GameSettings устанавливает 1280×720
```

**Причина 3:** Настройки сохранены из предыдущего запуска
```
Решение: Удалить %USERPROFILE%\AppData\LocalLow\DefaultCompany\DragRace\
```

### Кнопка не кликается:

**Причина 1:** Нет EventSystem
```
Проверка: Искать в логах "EventSystem найден"
Решение: Добавить EventSystem на сцену
```

**Причина 2:** Нет GraphicRaycaster на Canvas
```
Проверка: Проверить сцену Start.unity
Решение: Добавить GraphicRaycaster
```

**Причина 3:** Button.onClick не настроен
```
Проверка: Искать в логах "Слушателей: X"
Решение: StartSceneController должен добавить слушателя
```

**Причина 4:** Image.raycastTarget = false
```
Проверка: Искать в логах "Raycast Target"
Решение: Установить raycastTarget = true
```

---

## 📊 ЛОГИ ДЛЯ ПРОВЕРКИ:

### Правильные логи:
```
===========================================
🔧 GAME SETTINGS INITIALIZED
===========================================
📊 Resolution: 1280x720
🖥️ Fullscreen: False
...
===========================================
🟢 [StartSceneController] Start вызван
📍 Сцена: Start
🎯 Target: MainMenu
🔍 Кнопка найдена автоматически: StartButton
✅ Кнопка настроена: StartButton
📊 Слушателей: 1
🎨 Color: RGBA(0.200, 0.600, 0.200, 1.000)
✅ Interactable: True
✅ Raycast Target: True
===========================================
```

### При клике:
```
===========================================
🎮 [StartSceneController] КНОПКА START НАЖАТА!
🔄 Переход в: MainMenu
===========================================
✅ Сцена MainMenu существует
```

### Автотесты:
```
╔══════════════════════════════════════════════════════════╗
║         🧪 АВТОМАТИЧЕСКИЕ ТЕСТЫ ЗАПУЩЕНЫ                 ║
╚══════════════════════════════════════════════════════════╝
✅ ТЕСТ 1: Canvas найден: Canvas
✅ ТЕСТ 2: EventSystem найден: EventSystem
...
```

---

## 🎯 ДЕЙСТВИЯ ПОСЛЕ СБОРКИ:

**1. Проверьте дату файла:**
```
dir "D:\QwenPoekt\DragRace\Builds\DragRace_v0.5\DragRace.exe"

Дата должна быть СВЕЖЕЙ (только что)
```

**2. Запустите игру:**
```
DragRace.exe
```

**3. Проверьте окно:**
```
Если на весь экран:
- Alt+Enter (переключение fullscreen)
- Или проверьте логи
```

**4. Проверьте кнопку:**
```
Если не кликается:
- Нажмите Enter/Space (альтернатива)
- Или проверьте логи
```

**5. Отправьте отчёт:**
```
=== ОТЧЁТ ===

Окно: 1280×720 / На весь экран
Кнопка: Видна / Невидна
Клик: Работает / Не работает
Enter: Работает / Не работает

Логи: [есть проблемы / чисто]
```

---

**ЖДУ РЕЗУЛЬТАТОВ!** 🔍
